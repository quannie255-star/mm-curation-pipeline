# 开发计划（活文档）— AI 协作单一事实源

> **本文件是所有开发会话的入口与出口**：
> - 开工：先读本文件「当前状态」与「下一阶段任务」，按 docs/AI_CODING_PROTOCOL.md 走设计门；
> - 收工：**必须**回写本文件的「开发日志」（加一行）并更新任务状态，否则视为本轮未完成。
> 长期愿景与历史背景看 docs/ROADMAP.md；架构决策看 docs/ARCHITECTURE_V2.md；
> 环境与命令看 docs/RUNBOOK.md。

## 当前状态快照（2026-09-03）

| 阶段 | 状态 | 核心数字 |
|---|---|---|
| Week1-4 主线（图文管道） | ✅ | 清洗 R@1 +21%；分层采样再 +18~24%；486 脏数据召回 100% |
| Phase 2 P1-P10 | ✅（P6 暂缓） | 检测器泛化 87.3%；跨集去污染召回 94.4%；PSI 换源告警 0.36-0.66 |
| V2 α 协议收口 | ✅ | Sample 协议 + 注册表元数据 + Executor 抽象；112+29 测试绿 |
| V2 β 文本语料实例 | ✅（commit 73d0227） | 30.2 万维基语料；去重 10 万档 exact 1.0/near 0.97/21s；微调 ppl 差 +7.5%；全量漏斗 302,002→181,980 |
| V2 γ Ray 执行层 | ✅（本会话） | 等价性三口径全过（kept 集/StageStat/逐 id 分数）；10 万档 local 24s / ray 90s；簇代表 id 规范化（跨运行时确定）；包 34 + 主仓 133 测试绿 |
| V2 δ LLM-judge | ✅（诚实阴性） | 机制全链路通；0.5B 判官 κ(t) 全域 ≈0 不合格——kappa 协议把它筛出来（决策 7 实证）；换大模型只改 base_url |
| V3 θ 偏好判官工坊 | ✅ 模拟用户账达标（2026-09-06）；真人账待点击 | 通用 0.532 → 工坊判官 **0.839**（冻结 77 题，+30.6pp）；学习曲线：188 对未学会 / 488 对达标（笔记 #64）；真人标注闭环通道 v2 全文落盘 |
| V3 η 偏好闭环 + 迁移验证 | ✅ η-a/η-c 达标；⚠️ η-b 首轮未达标（2026-09-05） | η-a：双判官命中率 0.933/0.867 + 分歧率 0.783；η-c：域外 κ 0.178；η-b：抽取忠实性判官 0.53/0.50/0.41 ≈ 通用——0.5B 能力下限，转化「判官能力矩阵」概念，重训待 1.5B+ |
| V3 ζ 个人微调平台 | ✅ 首战达标（2026-09-03） | **钱表：通用 κ=-0.024 → 微调 κ=+0.560（R=0.960，解析率 100%）**，PRD 验收 ≥0.5 达成；PRD 见 docs/PRD.md |
| V2 ε 数据 CI | ✅（本会话） | data_ci_benchmark 门禁：exact 1.0 / near 0.954 / 误杀 0；劣化注入实测变红；双徽章（代码+数据）；损伤强度按 α 方法论标定 |
| 工程门禁（CI / lint / 文档） | ✅ | CI 覆盖包侧测试 + packages lint；ruff pin；README 同步 V2 β |
| V4 α 医疗模态协议扩展 | ✅（2026-09-17） | fhir_resource 第三模态零特例接入（包 v0.3.0）；合成 FHIR R4 500 条+150 注入，五算子主靶 recall 100%/误杀 0%，漏斗门禁 PASSED |
| V5 α 工业传感器增强包 | ✅（2026-09-17） | industrial_sensor 第四模态零特例接入（包 v0.4.0）；基座+领域增强包叙事确立（docs/DOMAIN_PACKS.md）；合成 1056 窗+317 注入，漏斗召回 100%/误杀 1.04% PASSED |
| V6 α 改写通道 + 归一化层 + 判决书（W1） | ✅（2026-09-20） | 包 v0.5.0：与 `Operator` 并列的 `Transformer` 协议 + `run_pre_stages`；主仓七规则归一化层 + `verdict.jsonl` 判决书 schema v1；A/B 全量 2066 篇双跑逐字节相同；UI 扩到 8 页签 |
| V6 补表 v2：JD 驱动的能力对齐（J1–J5） | 📋 设计完成，**等确认动码** | 重抓 10 个在招岗位（5 社招 + 5 校招，2026-09-20）→ 18 条要求聚类 → 按仓库实点映射：**✅8 / 🟡7 / 🔴3**。三条缺口全是**能力外延**（数据合成与增强 5/10、视频音频 4/10、SQL 数据资产化 3/10），**无一条是清洗能力本身**（清洗是 10/10 岗位都提、而项目 ✅ 最密集的那一栏）。新增 J1 合成/增强通道（1→N，唯一需协议变更，三表已落）/ J2 视频音频 / J3 SQL 目录层 / J4 语料体检 / J5 生态互通 |
| V6 W2 抽取层 + RawDoc + 源接入协议化 | ✅（2026-09-20，W2-6 人工抽检待填） | `extract/` 五个文件：`RawDocStore`（内容寻址 gzip + 边车 + 哈希校验 + 原子写，幂等）/ `Extractor` 协议（`tier`0/1/2 + `requires` 探测 + 失败原因四分类 `ExtractAttempt`）/ `heuristic`（stdlib 兜底，**永远可用**）/ `trafilatura`（未装记 `unavailable` 跳过）/ `news_cn`（W2-4 收编，**搬实现不复制**→逐字等价是结构保证）；`data/connectors.py`：`SourceConnector` 三实现（静态列表/RSS+Atom/Sitemap，含 `max_sitemaps` 上限）+ `spec()`↔`from_spec()` 往返无损 + `ingest()`。**对照实验（60 篇真实页面）**：站点档 49/60、兜底器 60/60、trafilatura 未装；**救回率 100%（11/11）/ 丢失率 0%，但字数比 1.92×、多抽段占比 88.9%** → **兜底器保证「从不失败」，绝不能当默认**。`--from-rawdoc` 可零网络重跑改指标 |
| **V6 P2 修复 R1–R6：真实数据判据**（用户最高优先级） | ✅（2026-09-20） | 合成 100% → 真实召回 24~72% / 误杀 16~28%，根因是**五个工业算子的判据全是对着合成形态写的**（形状要么不存在=空转，要么同形态另有含义）。**六项全做**：R2 `sensor_range` 表外通道由「伪装通过」改记 `None`；R6 抽检导出 + 阈值扫描固化进 `eval_real_sensor.py`；R1 `sensor_stuck` 改「持续尺度塌陷 + 无信息通道豁免 + **设备级停机豁免**」四级判据；R3 `fault_vs_maintenance` 三形态并集（哨兵 / 设备停机 / 跨度缺口）；R5 合成污染器补三类真实形态 + 低脏率档；R4 `sensor_drift` 增 MAD 尺度（**可选档**，默认仍 pooled）。**实点：C-MAPSS stuck 误杀 30.58%→2.44%；MetroPT-3 stuck 4.36%→1.07%、drift 28.90%→3.98%（同召回档降 75%）、首次抓到真脏 300；SKAB w64 drift 24.48%→1.03%**。**合成门禁全程逐位锁基线**（`123/0、128/8、184/20、63/0、57/0`，召回 100%、误杀 1.23%；MAD 档合成轨也更优：误杀 1.23%→0.95%、drift 20→0）。新增能力：**判据适用性**（「恒过」≠「通过」，MetroPT-3 上 `fault_vs_maintenance` 0/41475 无适用性、`unit_consistency` 0/7 组无适用性）。新建 `configs/funnel_industrial_real.yaml`（既有 4 个 config 一字不动） |
| V6 前端 F1/F2/F3：真实数据可视化 | ✅（2026-09-20） | **F1** 预计算 `data/reports/real_sensor_interactive.json`（2.39 MB，三数据集 × 窗级时间线/曲线/清单/适用性）；**F2** 门户加**第 9 页签「真实数据」**（数据集切换 + 旧↔新判据并排读数 + 召回-误杀权衡面 altair 曲线带当前点 + 误杀清单可筛可导出 CSV + 窗级三分解 + 适用性/可达性表）+ 视觉升级（首屏读数量规条）；**F3** 自包含零外部依赖单页 `docs/real_data.html`（2.42 MB，模板抽成 `scripts/templates/real_data_page.html`）。**顺带修两处口径自相矛盾**：`DOMAINS["image"]["blurb"]`「11 级滤芯」实点是 **12**（11 是 text_article 的数量——串档）→ 改为从注册表现算 + 测试绑死；预计算 JSON 里没被消费且与数据编码打架的 `state_code` 删除 |

**测试基线**：主仓库 **414** + 包 **67**（2026-09-20 P2 修复收官实点 `--junitxml` 解析；0 失败/0 错误/0 跳过；本机含 ray 口径）。上一版基线为 387（V6 W2 实点），387 → 414 的增量 = **R1–R6 真实数据判据修复 +15** + **F2 第 9 页签 +12**（净增，含一条重命名）。未装 ray 的环境/CI 包侧为 62。ruff check 双仓全绿。任何提交不得低于该基线。
> 包的 67 含 `test_ray_executor.py` 的 5 条——该文件 `importorskip("ray")`，**未装 ray 的环境
> （含 CI）整文件不收集**，所以 62 是 CI 口径；装了 ray 才是 67。文档里长期写 40 属于「凭记忆沿用旧数」。
> 基线数字历史上被写错过五次（133+34 → 140+40 → 151+40 → 175+40 → 263+54），根因是凭记忆填而不是点数。
> **改测试后直接用 `pytest --collect-only -q` 点数回写**，别沿用上一版的数字。
> 附：本机 `pytest -q` 的汇总行会被沙箱的批量删除护栏（`atexit` 时 `SystemExit(1)`）吞掉——
> 要准数就用 `--junitxml` 落文件再解析，别去数进度点（进度点还会被 `-q` 的百分比取整骗）。

**仓库**：github.com/quannie255-star/mm-curation-pipeline（main）。
> **推送排障见 docs/RUNBOOK.md §0.1**（两个独立故障：SSL 证书墙 / 凭据助手挂起，现象相似但修法无关）。
> 2026-09-03 已根治凭据挂起（global 层空值重置 + wincred），现在裸跑 `git push` 即可，不需要任何 `-c` 参数。
> 一秒自测：`git ls-remote origin main` 秒回 = 网络与 SSL 正常，push 挂住必然是凭据环节。

## 下一阶段任务分解

顺序：**γ → δ → ε**（γ 是 δ 的依赖：LLM-judge 批量推理需要分布式执行层撑成本模型）。
每个任务开工前先按 AI_CODING_PROTOCOL 写设计表进 docs/design_tables.md，经用户确认后再动码。

### γ Ray 执行层（预估 1 周）

| 任务 | 内容 | 验收标准 | 预估 |
|---|---|---|---|
| γ0 | Ray spike：本机 `pip install ray`（Windows 兼容性实测）+ ray.data/map_batches 跑通 1 个文本算子；若 Windows 不可用则改 WSL2 或降级「多进程池执行器」并记录决策 | spike 结论 + 决策记录（走哪个路线） | 0.5 天 |
| γ1 | `RayDistributedExecutor` 实现 α 已有的 Executor 协议（懒加载 import，不装 ray 零依赖可跑；对照 ARCHITECTURE_V2 决策 2 的方案 B 双实现） | 注册后 config 一行切换运行时；不装 ray 的环境全量回归绿 | 1 天 |
| γ2 | shardable 语义落地：分片算子 map_batches 批间并行；非分片算子（text_minhash 等全局视角）单节点汇聚执行 | 8 级文本漏斗在 Ray 下跑通，去重结果与本地一致 | 1 天 |
| γ3 | 等价性验收：同一 config 本地 vs Ray，cleaned.jsonl 逐条一致 + funnel_stats 各级数字相等；双跑耗时对比表（10 万档） | 等价测试绿 + 基准报告落 data/reports/ | 1 天 |
| γ4 | RUNBOOK γ 段 + ROADMAP 回填 + ENGINEERING_NOTES（Ray 踩坑）+ 本文件日志 | 文档齐，推送 | 0.5 天 |

### δ LLM-judge（预估 1 周）

| 任务 | 内容 | 验收标准 | 预估 |
|---|---|---|---|
| δ0 | 设计门：服务形态（vLLM OpenAI 兼容服务，本机 8GB 显存选小模型如 Qwen2.5-1.5B/3B）、抽样协议（L1/L2 高分歧区间优先抽样）、评分 rubric 与解析格式 | 设计表经用户确认 | 0.5 天 |
| δ1 | `LlmJudgeOp`（CostClass.LLM 档，抽样率配置化）：批量调服务 + 分数解析 + 失败降级策略（服务不可用时跳过不阻塞） | 单测（mock 服务）+ 真实服务冒烟 | 1.5 天 |
| δ2 | 一致性评测：judge vs 规则/模型算子的 Cohen's kappa + 分歧样本抽样分析报告 | kappa 报告落 data/reports/，分歧案例进笔记 | 1.5 天 |
| δ3 | 接入漏斗为可选 L3 级（config 开关）+ 验收测试 + 文档四件套 + 本文件日志 | L3 开启时漏斗跑通，关闭时与 β 基线一致 | 1.5 天 |

### ε 数据 CI（预估 3 天）

| 任务 | 内容 | 验收标准 | 预估 |
|---|---|---|---|
| ε1 | 新 workflow `data-ci.yml`：定时/手动触发小规模污染基准（1 万档），断言 P/R 不低于门限（exact ≥0.99 / near ≥0.90） | Action 绿；人为注入劣化能变红 | 1 天 |
| ε2 | 阈值回归门：threshold_scan 关键算子拐点偏移超警戒即失败（防上游换源静默漂移）→ ✅ **2026-09-03 第七会话收官**：`scripts/threshold_regression_gate.py` 曲线对冻结基线 `configs/threshold_baseline.json` 逐点比对 | 门限逻辑 + 测试 | 1 天 |
| ε3 | README 徽章 + 失败通知说明 + 文档四件套 | 推送后徽章显示绿 | 1 天 |

### ζ 个人微调平台（首战「专属数据判官」，PRD 见 docs/PRD.md）

| 任务 | 内容 | 验收标准 | 预估 |
|---|---|---|---|
| ζ1 | 数据获取器：新闻源爬取（robots 合规/限速/幂等）→ Sample 协议 | ≥2000 篇正文；重跑不重复 | 2 天 |
| ζ2 | benchmark 构建器：域评测集版本冻结 + 防污染去重 + 准入指标 | ≥300 条 + manifest；泄漏检查测试 | 3-4 天 |
| ζ3 | LoRA 微调器：peft + Qwen2.5-0.5B（本机 8GB）+ 实验 ledger | 本机跑通，配置/loss 落盘 | 3-4 天 |
| ζ4 | 评测 runner 串联 + 端到端案例 | κ ≥0.5（对照 0.013）；域外泛化如实报告 | 2 天 |
| ζ5 | 文档四件套 + RUNBOOK + 收官 | 全链路测试绿，一键复现 | 1 天 |

### 候补池（γδε 完成后或穿插）

- ~~跨模态统一：图像漏斗迁移到 V2 Sample 协议~~ → **盘点结论：已完成，债务记录作废**（2026-09-03 第四会话实点）。
  19/19 个注册算子**全部**已声明 V2 元数据（纯图像 8 / 纯文本 7 / 双模态 4），图像漏斗
  `configs/pipeline.example.yaml` 经 `Sample.from_dict` + V2 Executor 执行——「α 只迁了 12 算子中的
  文本/通用部分」是 α 当时的快照，β/γ/δ 期间已补齐，**候补池记录未同步**。
  盘点脚本：`pytest --collect-only` 之外的快速核对见本条目下方命令。
  **但盘点翻出两个此前没记的真缺口**（已立为候补池前两项）：
  盘点命令（下次核对直接跑，别再凭记忆）：

  ```bash
  python -c "
  import sys; sys.path.insert(0,'src'); sys.path.insert(0,'packages/curation-eval/src')
  import mm_curation.operators
  from curation_eval.registry import available_operator_metas as M
  m = M()
  for n in sorted(m): print(n, sorted(m[n].modalities), m[n].cost_class.name, m[n].shardable)
  print('总计', len(m))
  "
  ```

  1. **图像漏斗 × Ray 等价性未验证**：γ3 只跑了 `configs/text_funnel.yaml`，图像漏斗从未在 Ray 下跑过。
     风险集中——图像侧有 3 个 `shardable=False` 全局算子（md5_exact / phash_near / semantic_dedup），
     而 γ3 首跑失败正是栽在全局去重算子上（text_minhash 簇代表依赖块序）。**phash_near 大概率同坑**。
  2. **ε 数据 CI 未覆盖图像去重**：`data_ci_benchmark.py` 只锁了文本 `dedup_fast.dedup_texts`，
     md5/phash 的召回-误杀没有任何门禁，换源漂移不会被抓住。
- ~~[新] 图像漏斗 × Ray 等价性验证~~ → ✅ 已完成（2026-09-03 第五会话）：设计表入 design_tables.md
  （γ3 三口径 + 新增第四口径 dedup 标记）；实测 **2106 条全量双跑四口径全等**（kept 1608 相等 /
  StageStat 相等 / 逐 id 分数 0 不一致 / dedup 标记 324 条相等），local 65.4s vs ray 137.3s。
  报告 `data/reports/ray_image_funnel_benchmark.{json,md}`；等价性测试 `tests/test_image_ray_equivalence.py`。
  **附带发现**：id 排序约定使簇代表 = 字典序最小 id——注入复制品（id 小于原始图）会成为簇代表，
  与污染模块「种子在前」的输入序约定相反；kept 集质量不受影响（重复各留一张），但 duplicate_of
  的指向会从原始图变为注入样本，审计时需知晓
- ~~[新] ε 门禁扩到图像去重~~ → ✅ 已完成（2026-09-03 第六会话）：`scripts/data_ci_image_benchmark.py`
  锁 md5_exact / phash_near——2800 张合成图（2000 base + 300 exact 字节复制 + 500 near
  裁剪重编码注入）真跑漏斗算子，**exact 1.0 / near 0.994 / 误杀 0/2000 → GATE PASSED**；
  劣化注入（裁剪加重到 10~20%）实测 near 0.152 → **exit 1（门禁会红）**。
  已接入 data-ci.yml。标定花三轮收窄损伤（块状合成图对裁剪比真实照片敏感，
  V1 的 4~10% 参数下 15.6% 超阈，收到 1~5% 后 near 距离 p90=8），门限 0.97
  留 2.4pp 劣化余量。RUNBOOK §1.9.1 / INTERVIEW 杀牌 E 已回写
- ~~INTERVIEW.md 融合 β 新弹药~~ → ✅ 已完成（2026-09-03 第二/三会话）：见「七、V2：从一条管道到一个框架」——已含 α/β/γ/δ/ε 五张杀牌（δ 的 κ 与 ε 的数据 CI 数字已于第三会话补入）+「八、V3 个人微调平台」；**ζ 出 κ 数字后需回填第八节进度表与第七节 STAR 表**
- **README 状态段补 V3 ζ**（立项一句话 + 进度 + ζ 数字）——ζ 收官时由 ζ 负责人回填。当前 README 仍写到「V2 全阶段完成」，未提 V3/ζ；第三会话已先对齐两处口径：测试数 129+29 → 140+40、工程笔记 53 → 55 条（与 INTERVIEW.md/ROADMAP 一致）
- P6 规模扩展（暂缓中，见 ROADMAP；注：β 已把语料规模从 1.6k 推到 30.2 万，P6 的紧迫性下降——面试时可直接用 β 的 187 倍规模跨度作答）

## 协作硬规则（任何 AI/开发者必守）

1. **进场顺序**：本文件 → docs/AI_CODING_PROTOCOL.md → docs/ARCHITECTURE_V2.md（相关决策）→ 对应模块代码。
2. **设计门**：非平凡任务先写设计表（design_tables.md）等确认；<20 行 bug 修复可跳过但须简述。
3. **质量门（每次提交前）**：`python -m ruff check .` + `python -X utf8 -m pytest -q --tb=no`（主仓库）+ `packages/curation-eval` 下同样跑 pytest——三者全绿才算完成。
4. **收工回写（强制）**：更新本文件任务状态 + 开发日志加一行（日期/内容/关键数字/commit）；有面试价值的现象写 docs/ENGINEERING_NOTES.md（格式：现象→根因→决策→话术）；阶段级进展同步 ROADMAP.md 进度表。
5. **环境坑速查**（详见 RUNBOOK/FAQ）：用系统 Python 3.11（.venv 已坏）；中文输出加 `-X utf8`；Git Bash 无 make；HF 走 hf-mirror.com 带浏览器 UA；模型权重加载必须走 `mm_curation/gpt2_weights.py` 的本地 safetensors 入口（CVE-2025-32434，禁止直接 torch.load .bin）；JSONL 读取一律 `read_text().split("\n")`（禁 splitlines）。
6. **提交规范**：中文 commit message，首行阶段前缀（如「V2 γ：…」）；数据/模型/报告产物不入库（.gitignore 已配，报告用 RUNBOOK 命令重生成）。
7. **不做的**：真前端、W&B 云依赖、算子市场等长期愿景（见 ROADMAP 末尾）。

## 开发日志（新会话在前，每次开发必加一行）

| 日期 | 会话内容 | 关键数字/结论 | commit |
|---|---|---|---|
| 2026-09-22 | **R7–R11：真实数据「清洗质量」修复（半合成注入评测 + 数据包络 + 覆盖率闸门）**。用户指令原话：「帮我继续完善一下重点解决没法清洗数据这件事，不需要结果多么好看但是清洗质量不要这么差，可以参考已经有的项目」。**①R11 先造出有效真值**（这是本轮的地基）：三个真实数据集的标签（C-MAPSS `degraded` / MetroPT-3 `fault_window_air_leak` / SKAB `sensor_anomaly`）**全是过程异常**，拿它当召回分母 = 用 DQ 检测器去考故障诊断（§8.10 根因 A）。新脚本 `scripts/eval_real_injection.py` 按业界做法（把已知缺陷注入真实录波）实现**差分三口径**：`recall_injected`（分母=注入集，**唯一可对外讲的召回**）/ `collateral_false_drop`（注入造成的附带损伤，应≈0）/ `background_flag_rate`（这份数据**自带**的异常率上界，**不是误杀率**）。只注入数据质量型五形态，**不复用包内现成污染器**（它们在真实窗上供体池为空：`_beyond_baseline` 要 ≥5 个更早窗 / `out_of_range` 要内嵌量程表 / `unit_swap` 只认 MPa）。途中修掉三处注入侧口径错误：注入必须**替换**原记录（追加会让同 `(设备,时刻,通道)` 重复 → MSPC 参考集被污染）、**不注入参考段**、判决在时刻级而**丢弃必须在通道级**（整行丢让附带误丢 758→0）。**②R8 补上 `sensor_range` 缺失的输入**：该算子在三个真实数据集上**一次都没评过**（通道全在手册量程表外 → 记 `None`），注入 1888 条已知越界缺陷召回 **0.0%** —— 问题不在阈值、在**缺输入**。新脚本 `scripts/build_envelopes.py` 从**参考段**（每组最早 30% 窗，工控 golden batch 口径）按 `中位数 ± margin×1.4826×MAD` 定**读数级**边界（不是窗均值级——那会小一个 `√n` 量级），跨设备/工况取**并集**（个体与工况差异属合法范围），带 `n_ref` 且低于 15 的边界不许用。**③一处设计被 MetroPT-3 推翻（本轮最有价值的发现）**：同样配方在 C-MAPSS / SKAB 上包络背景率仅 1.16% / 0.66%，在 MetroPT-3 上却是 **54.40%**（漏斗级 56.29%）—— **一半以上真实数据被判成脏**。根因：固定参考段隐含**「数据稳态」这个未声明假设**，而 MetroPT-3 是真实压缩机、负载与启停在持续变化（前两个数据集是仿真退化/小试验台，**掩盖**了它）。修法：给**生成端**加**留出集自检**——边界仍只由参考段拟合，但用参考段**之外**的读数做接受/拒绝检验（留出集验证，不是「用整份数据调边界」，不构成同义反复），越界率 >5% 的组**整条边界不写入**、宁可让判据记未评。实测 MetroPT-3 的 `Motor_current` 留出集越界率 48.9% 被拒 → 包络背景率 **54.40% → 0.43%**，同时漏斗注入召回 **81.2% → 89.9%**。**④R10 覆盖率闸门**：`sensor_range` 报告里那行 `0.0%` 曾被读成「零召回」，实际是**没有分母**（更早一版把表外记 `1.0`，读起来像「量程全部通过」，同样一次没评）→ 评测报告给每个算子**同时印已评覆盖率**与召回，覆盖率 <50% 或为 0 时标 `ALL_UNSCORED` / `LOW_COVERAGE` + `<<< 召回不可引用`，结论里写明「不是零召回，是没有分母」；`explain` 把 `no_range_entry`（去找手册量程）与 `insufficient_reference`（等更多参考数据）两种未评分开。**⑤顺带修掉一处真实的分母口径错误**：背景异常率的分子含「被注入替换掉的原窗」而分母不含 → 系统性虚高，修正后三数据集漏斗背景率各降约 0.4pp（6.17→5.74 / 6.48→6.05 / 4.46→4.06）。另把稳健统计三件套抽到 `src/mm_curation/operators/robust.py` 作**唯一真相源**（MSPC 控制限与量程包络两个消费者，抄两遍必然漂移；`sensor_drift` 的 `1.4826` 字面量也接过来），并修掉一处文档腐烂（`_MSPC_MARGIN` 注释仍写「参考集 99 分位」而实现早已改稳健限）。**⑥R7（基线长度自适应）如实不做**：真实数据的主要瓶颈不是基线长度而是「稳态假设」本身，在错误假设上做微调无意义；正解是**滚动基线**（EWMA/移动窗），属另一套设计、需单独对照实验。**⑦门禁与测试**：合成门禁**逐位锁基线**（`123/0、128/8、184/20、63/0、57/0`，召回 100.0%、误杀 1.23%）证明改造对既有 4 个 config 零影响；`tests/test_sensor_quality.py` 新增 9 条（稳健限两条性质、包络优先级/参考不足/形状报错、留出集自检拒绝、跨设备并集、**生成端↔消费端格式契约往返**），并同步一条既有断言的期望值（`explain` 新增三个区分未评原因的字段）。**⑧三数据集完整档终版**（注入率 10%、seed 42）：漏斗注入召回 C-MAPSS **76.6%→85.2%**、MetroPT-3 **81.2%→89.9%**、SKAB w64 **87.8%→93.2%**；`out_of_envelope` 分形态召回 26.2→62.2 / 11.4→54.9 / 59.3→84.8；附带误丢 801→757 / 225→217 / 116→68（不升反降）；**每个注入形态都有算子实质覆盖**。笔记 **#75（教科书的「参考集 99 分位」在小样本上静默空转，注入 330 条召回 0.0%）/ #76（未评与零召回在报告里长得一模一样）/ #77（用数据分布定边界遇工况漂移会判掉一半真实数据 → 留出集自检）**；设计表 §8.11 | 漏斗注入召回 **85.2% / 89.9% / 93.2%**（原 76.6% / 81.2% / 87.8%）；`sensor_range` **0.0%→35.1% / 25.2% / 43.5%**；包络自检把 MetroPT-3 背景率 **54.40%→0.43%**；合成门禁 **逐位不变**（100.0% / 1.23%）；测试 **423**（+9）、包 **67** | 本次提交 |
| 2026-09-20 | **V6 P2 收官：真实工业数据判据修复（R1–R6）+ 前端 F1/F2/F3**。用户指令：「先解决处理清洗真实工业数据效果不好这一问题，其他的可以先放一放，然后给我一个漂亮的可交互前端」；三处待裁决经用户确认（修复范围=**六项全做 R1–R6**、前端=**Streamlit 第 9 页签 + 自包含 HTML 单页两者都要**、R4 回退策略=**可选档**）。**①先实点再动码，两处推翻了设计表原判**：(a) R3 原设计要「ingest 产 `gap_window` 记录」，实点后否掉——payload 字段已够算子侧自算，且 MetroPT-3 上 `span_ratio>5` 的 101 窗带故障标签仅 **2/101 = 2.0%**，与基准故障率 **2.0% 相同**（统计独立），按小时聚合集中在 19 时(29)/0 时(20) → 是**每日停机**不是故障，故改为窗内跨度自检验（不动包的 `KNOWN_RECORD_TYPES`）；(b) `sensor_stuck` 的「设备级停机」豁免**不在原设计里**，是跑真实数据时被 1337 条「误杀」逼出来的。**②R1**：`sensor_stuck` 三级→**四级判据**（⓪多通道同步恒定的设备停机 → `None` ①无信息通道豁免 ②严格平坦 ③持续尺度塌陷），参考尺度取健康窗 σ 的 p75（**已知上界：塌陷窗占比 >75% 时参考尺度被自己拉低 → 已钉成断言**）。C-MAPSS 误杀 **30.58% → 2.44%**。**③R3**：`fault_vs_maintenance` 由「恒过」改**三形态并集**（全哨兵 `-999` 合成靶子一字不动 / 设备级停机 → `1.0`+证据 `machine_stop` / 跨度缺口 → `None`+证据 `span_gap`）；与 `sensor_stuck` 共用 `_device_stop_keys()` 单一真相源——**两个算子对同一批窗给不同答案是「对的」**（一个答「不适用」，一个答「不是故障是停机」）。**④R2/R6**：`sensor_range` 表外通道从 `return 1.0`（伪装通过）改记 `None`，SKAB 1048/1048、C-MAPSS 29757/29757、MetroPT-3 41475/41475 首次如实显形；新增 `judge_applicability`（判据**适用性**：靶子有没有）与既有 `judge_reachability`（**可达性**：门槛够不够）构成同一句话的两个分母——MetroPT-3 上 `fault_vs_maintenance` **0/41475 无适用性**、`unit_consistency` **0/7 组无适用性**、`sensor_stuck` 适用（1337 严格平坦，其中 191 是设备停机）。**⑤R5**：合成轨补三个污染器（噪声平坦线/采样停顿/信息黑障），用于给新档造靶子；合成门禁**逐位恢复基线**（`123/0、128/8、184/20、63/0、57/0`）。**途中钉死两条结构性事实**：(i) 「注入即复制」使「连续 N 窗」类判据在合成轨**结构性不可达**（最长游程仅 3~5 vs 门槛 10），写成防回归断言；(ii) 浮点残差陷阱——`[1.2]*256` 的 σ ≈ 5.8e-15 而非 0，「平坦」只能判 `max-min == 0.0`。**⑥R4**：`sensor_drift` 增 MAD 尺度（`1.4826×MAD(同组窗均值)`，取 `max(mad, pooled)` 作地板 → **只会更宽松，不可能引入新误杀**）；同召回档误杀 MetroPT-3 **28.90%→7.24%**、全档 **28.90%→3.98%**、C-MAPSS 18.96%→1.22%、SKAB w64 24.48%→1.03%；**合成轨 MAD 也更优**（误杀 1.23%→0.95%、drift 20→0）→ 回退条件未触发，但仍**按用户决定以可选档交付**（新建 `configs/funnel_industrial_real.yaml`，既有 4 个 config 一字不动）。**⑦前端**：F1 预计算 JSON（2.39 MB）+ F2 门户**第 9 页签「真实数据」**+ 视觉升级（读数卡量规条；**不画「趋势条」**——报告里没有时间序列，硬画就是编数据）+ F3 自包含零外部依赖单页 `docs/real_data.html`（2.42 MB；把 492 行内嵌 HTML 从 `.py` 抽成 `scripts/templates/real_data_page.html`，同时根除 33 处 ruff E501，**产物逐字节等价已验**）。**⑧修掉两处口径自相矛盾**：门户「11 级滤芯」实点是 **12**（11 恰好是 text_article 的数量——**串档**）→ 改为从注册表现算并加测试绑死；预计算 JSON 里**无消费者且与数据编码打架**的 `state_code` 删除（改成如实描述各数组的 `columns`）。**⑨如实标注的两条边界**：SKAB **256 窗档**实测 1048 窗、5 算子**全部零动作**（而 64 窗档命中 479）——既有事实、本次调查确认，**原因未定，故不进前端**（避免在页面上放一个问不住的数），已列待查；`data/raw/real/`（383MB）**仍未入 .gitignore**（GAP_AUDIT P0-1 仍在），本轮未动 | 真实误杀：C-MAPSS stuck **30.58%→2.44%**、MetroPT-3 stuck **4.36%→1.07%** / drift **28.90%→3.98%**、SKAB w64 drift **24.48%→1.03%**；MetroPT-3 首次抓到真脏 **300**；未评如实显形（MetroPT-3 `sensor_range` **41475/41475**）；合成门禁逐位锁基线（召回 100%/误杀 1.23%）；测试基线 **387 → 414**（+15 R 系列 / +12 F2）+ 包 **67**，ruff 双仓绿；新增笔记 **#67–#70** | 本次提交 |
| 2026-09-20 | **V6 W2 执行（抽取层 + RawDoc + 源接入协议化）——三项全落，W2-6 首次跑出真实对照数字**。用户「开始执行吧」。**①W2-1 RawDoc 内容寻址存档**（`extract/rawdoc.py`）：`data/raw/html/<sha[:2]>/<sha>.html.gz` + `.meta.json` 边车；幂等（同内容只落一份、只累积 urls）、原子写（`.tmp`→`os.replace`，笔记 #2）、`get()` 重算 sha 与路径比对、不符抛 `RawDocCorrupted`（**不静默降级**）；`stats()` 给出全层唯一数字。**②W2-2/2-3/2-4 三级抽取链**（`extract/base.py` + `heuristic.py` + `trafilatura_ext.py` + `news_cn.py`）：与 `Operator`/`Transformer` **并列**的 `Extractor` 协议（`name`/`tier`/`requires` + `__init_subclass__` 注册期 fail-fast）；tier0 站点专用（纯 stdlib）/ tier1 `trafilatura`（`requires` 探测 + 懒加载 + 未装记 `unavailable` 跳过）/ tier2 stdlib 兜底（**永远可用，CI 零新依赖**）；`ExtractAttempt` **失败也记且记原因**（`no_container`/`no_paragraphs`/`too_short`/`unavailable`）——只记赢家的话阴性结果无法归因。**W2-4 收编法：搬实现不复制**（`NewsCnExtractor` 成为唯一真相源，`web_sources.extract_article` 退化成薄适配），逐字等价因此是**结构保证**而非测试负担。**③W2-5 源接入协议化**（`data/connectors.py`）：`SourceConnector`（`listing_urls` + `parse_listing`）三实现——`StaticListingConnector`（正则）/`RssConnector`（RSS2+Atom，`rel=alternate` 优先）/`SitemapConnector`（`sitemapindex` 展开 + `max_sitemaps` 上限，**不设上限等于对源站做全量拉取**）；`spec()`↔`from_spec()` 往返无损（**`spec()` 初版漏了 `extractor`，按 spec 重建会把抽取器悄悄掉回默认链**——测试抓出来的真 bug）；XML 两道护栏（拒 DOCTYPE / 限长，本机无 `defusedxml`）；`ingest()` 与旧 `crawl` 在**同一假 fetch 下产出逐字节相同的 JSONL**（离线断言，无需联网）。**④W2-6 三口径对照首次跑出真实数字**（60 篇，`data/reports/extract_ablation.{json,md}`）：`news_cn` **49/60**（11 篇抽不出）、`heuristic` **60/60**、`trafilatura` 未装记 `unavailable`（**从分母剔除，不记 0 分**）；**救回率 100%（11/11）、丢失率 0%**，但**字数比 1.92×、多抽段占比 88.9%、基准段被覆盖 89.5%** → 结论是**兜底器保证「从不失败」，但绝不能当默认**（它把侧栏推荐位 40+ 条标题抽进正文）。**⑤追出两个机制级缺陷（真证据，非笼统结论）**：站点档的 `<a>` 剥离正则 `<a\s[^>]*>.*?</a>` 用 DOTALL 在整段上替换，会**删掉段落内部的行内链接文本**（导语开头的「中新社」署名整批消失，实测 `<p>　　<a ...>中新社</a>上海9月20日电…` → 缺「中新社」），并且**跨段的 `<a>` 会把多段内容拼成一段**（实测基准段在原始 HTML 里都不是连续子串）——这条路径正是产出既有 2066 篇语料的路径。**⑥指标自身也修了一次假象**：段落包含判断前必须 `html.unescape`（兜底器反转义 `Fijo&#322;ek`→`Fijołek` 而站点档不做），不归一会把覆盖率**系统性低估**（实测把 11% 的基准段错判为未覆盖）。**⑦离线重跑**：`--from-rawdoc` 只读已存档原文、零网络请求改指标——这是 W2-1 最直观的兑现。**质量门**：主仓 **387**（328+33+26）+ 包 **67**，0 失败/0 错误/0 跳过；ruff 双仓全绿。**未做（诚实标注）**：W2-6 的人工抽检 30 篇只产出了对照表（`extract_review_sample.jsonl`，`human_verdict` 留空待填），报告**不含人工质量判定**。 | 本次 |
| 2026-09-20 | **JD 对齐 v2：重抓 10 个在招岗位 → 18 条要求聚类 → 按仓库实点逐条映射**（`docs/JD_RESEARCH.md` 整篇重写）。v1 是 2026-08-20 写的、状态列还停在 Week1/2/3（项目已走完 V2–V6 α），本版**全部重新抓取**：字节 Seed（AI 数据与安全 · 大模型自动化评测实习）、字节（多模态大模型数据工程师，社招）、阿里云智能（AI Data 工程师 · MaaS 数据）、阿里 Astar（线上回流数据的大模型训练与评测体系，2027 届）、上海某（多模态数据工程师 · 基础设施方向）、AI 数据工程师（采集/清洗/分析）、月之暗面 Kimi、阶跃星辰 Stepstar（实习 + 校招）、B 站（多模态数据智能工程师，2027 届）、电子五所（大模型测评数据工程师）——**5 社招/全职 + 5 校招/实习**。**18 条要求聚类并按实点映射**（每格都能指出文件或 commit）：**✅ 有实证 8 条**（清洗/质检/去重 10/10 岗位都提、质量评估可量化 8/10、模型驱动质检 6/10、脱敏 PII 4/10 含通用 `pii_detect` + FHIR `phi_residual` + `wm_nsfw_cnn`、可视化 4/10、评测集防污染 3/10、服务化 CI 通用）；**🟡 部分 7 条**（采集解析→W2、血缘→W4、配比→W4、断点续跑→W4、可视化进包→W5、Bad Case 归因→W3、**数据回流飞轮未覆盖**）；**🔴 缺 3 条**。**本轮最重要的结论：三条 🔴 没有一条是清洗能力本身**——数据合成与增强（5/10）、视频/音频（4/10）、SQL/数据建模/数仓（3/10）**全是能力外延**；而「清洗/质检/去重体系」是 10/10 岗位都要求、且项目 ✅ 最密集的一栏 → **清洗这条线已经够深（29 算子 / 四模态 / 每算子可算 P:R），下一步该补的是出口。** **由此新增 5 个任务（`docs/design_tables.md`「V6 补表 v2」，等确认后动码）**：**J1 合成/增强通道（1→N 扩样）**——唯一需协议变更的一项，W1 刚建的 `Transformer` 只能 **1→1** 而合成与增强是 **1→N**，**通道形状不对**（与 W1「缺的不是算子，是一条通道」是同一类判断）；附一条红线：**合成样本必须走同一条漏斗**，否则「我合成了一批数据」不可证伪。J2 视频/音频模态 + 抽帧（复用图像算子链，扩模态的低成本路径）/ J3 DuckDB SQL 数据目录层（验收要求「用 SQL 复现一个现有报告数字且**逐位相等**」）/ J4 语料分布体检报告（多样性/冗余/缺失/长尾）/ J5 公开评测生态互通（只做导出/导入互操作，不做全套集成）。**明确不追**：EB/PB 实跑、Agent 行为数据与 RLHF 全链路、论文产出、PE/Agent 应用开发。**执行顺序**：W2（进行中）→ J1 → J3/J4 → W3 → W4 → W5 → J2 → J5。顺带修设计表「验收标准汇总」里一处过期基线（274+54 → **328+67**），并把 V6 表末尾的三处待裁决正式标记为**已通过**（用户 2026-09-20「按照你建议的来」） | 实点映射 **✅8 / 🟡7 / 🔴3**；JD **10 个**（5 社招 + 5 校招）；新增候选 **J1–J5**；7 条 🟡 中 **6 条已被 V6 计划覆盖** | 本次提交 |
| 2026-09-20 | **V6 α 执行（W1：让证据可信）——前置改写通道 + 归一化层 + 判决书 + 2 个页面**。用户批准设计表（「按照你建议的来，先把全链路搭出来再说，搭出来再看看怎么引入新的前沿技术」→ 差异化挂起、不追新技术）。**①协议层（包 v0.5.0）**：原计划「加个归一化算子」，实点后发现**框架缺的不是一个算子，是一条通道**——`Operator` 协议全是「打分→阈值」，没有能改写样本的通道（#65 修复记录原文已写「没有文本改写通道」，当时的结论错了：把它当缺算子，实际是协议缺一条）。硬做成算子会出现病态——改写算子排第 5 级，前 4 级已在**未改写**文本上打分。故新增与 `Operator` **并列**（非继承）的 `Transformer` 协议 + `TransformResult` 三工厂（replaced 强制非空 log，「没留痕的改写不可审计」）+ `run_pre_stages`；`run_funnel` 增 `pre_stages` 可选参数默认空 → **不改 Operator 签名、既有 config 一字不动**。**②归一化层（主仓）**：七条纯函数规则 + 语料级聚合 + `TextNormalizeTransformer`；**首跑三条单测红暴露规则职责重叠**（`whitespace_collapse` 的 `str.strip()` 顺带吃掉行尾 `\r` → 逐规则归因污染）→ 改为**规则正交**（`newline` 独占 `\r`、`control_chars` 不删 `\r`、行内空白正则排除 `\r\n` 且只 `.strip(" ")`），修正后 `whitespace_collapse` 976 / `newline` 891 各自干净。**③判决书（主仓）**：`verdict.jsonl` schema v1（`rule` 机器可读判据代号 + `evidence` 判据用到的量 + `input_fingerprint` 三格是「判决书」与「一行日志」的分界）+ PROV-O 三元 + `review` 人审回填位；用 `wrap_for_verdict` 包装算子（`isinstance` 语义不变，**不动执行器**，保住 γ 等价性验收语义）；**旁路产物**，三条红线单测锁死「不传 ledger 零痕迹」。**④A/B 对照（全量 2066 篇）**：7 个规则档算子中**只有 `text_minhash` 判决变化**（29→30，Δ=+1），存活 2032→2031；`chinese_ratio` **Δ=0**（#65 单点补丁已覆盖）；归一化改写 976/2066 篇（47.2%）、净删 **55.8%** 字符；**空白占比 p50 1.98%→1.94%（几乎不动）/ p90 79.89%→8.09%（差一个数量级）**、>50% 篇目 **849→0** → 语料**双峰**，归一化作用在长尾非中位数。**如实标注：归一化的确定增量不在拦截数/召回上（判据看不见它），在下游载体（字符总量/去重签名/困惑度输入）与架构正确性——不抢数字。** **⑤踩到并修掉一个真 bug（笔记 #71）**：重跑对照实验时报告数字变了——**一个纯格式化重构不可能改数字**，顺查发现**追加式台账 × 整文件聚合**相撞：`verdict.jsonl` 是追加写的，报告 `by_rule` 是整文件聚合的，每重跑一次多统计一轮（实测文件里已躺 4 轮，57748 = 4×14437；UI 的「这一级共 8264 个分数」是真实值 2066 的 4 倍）。修法：脚本「一次运行=一份干净台账」（**截断**而非删目录——沙箱批量删除护栏在本轮累计超阈值后会拦掉后续所有删除）+ **不变量断言**「台账行数 = 各滤级进入数之和」+ 报告正文露出该行数；验收用**同码双跑逐字节相同**。**⑥UI**：`showcase_app.py` 六页签 → **八页签**（⑦清洗过程 / ⑧阈值沙盘），新增 6 个纯函数；台账读取加 `st.cache_data` 包装（Streamlit 每次控件交互都重跑脚本）；**顺带修掉一个潜在 `NameError`**——阈值沙盘的指标卡原本渲染在 `if rec:` 之外，选到批量算子（`text_minhash` 无逐样本分数）时 `rec` 为 None → `c1` 未赋值 → 崩；已补「批量算子不适用」显式说明 + 回归测试（默认选中项恰好有分数，普通冒烟抓不到）。**门面数字腐烂一并修**：`showcase_app` 写「质检滤芯总数 24」（实点 `available_operators()` = **29**）、测试数「267 + 54」（实点 **328 + 67**）、README 写 175+40、AGENTS/DEV_PLAN/PROOF_CHAIN 各写一套 → 四处口径统一到 **328 + 67**，并把常量 + 实点日期写进代码 | 包 **v0.5.0**；主仓 **328** + 包 **67**（实点，0 失败/0 跳过，junitxml 计数）；ruff 双仓全绿；`whitespace_collapse` 976 / `newline` 891；`text_minhash` 29→30（唯一判决变化）；改写 976/2066、净删 55.8% 字符；空白 p90 79.89%→8.09%、>50% 篇目 849→0；双跑逐字节相同；不变量 14437 = sum(n_in)；算子实点 29；笔记 #69/#70/#71 | 本次提交 |
| 2026-09-20 | **V6 设计门：全链路补全 + 统一可视化工作台（docs/design_tables.md 落盘，等确认动码）**——用户指示「三域都保留；源接入、抽取层、人审层等缺失层级尽快补完；其他环节对标业界升级；中间过程前端可视化；保留业界完整架构但做成比业界更易上手/交互更多/更可视化的版本，并补业界空缺生态位」。**落表前全仓实点，推翻我自己上一轮的两处描述（先更正再设计）**：①`src/mm_curation/data/web_sources.py` **已存在**——`fetch`（UA+重试+退避）/`can_fetch`（robots.txt 按 host 缓存）/`extract_links`/`crawl`（限速+幂等+断点续爬）＋`extract_article`（硬编码 `left_zw` 容器与 `backtop`/`ydtj` 结束标记），训练 4 条测试 → **源接入/抽取不是「完全缺失」，是「站点耦合的手写正则半成品」**；②真实差距三条：**抽取站点耦合**（换源要重写函数）、**无 RawDoc 中间态**（HTML 抓完立刻出 text、原始 HTML 从不落盘 → 无法换抽取器重放、无法做抽取对照、无法复现 #65）、**归一化层缺失的真根因是框架只有「打分→阈值」一条通道、没有「改写」通道**（笔记 #65 修复记录原文：「盘点算子框架后确认所有算子都是打分器，没有文本改写通道」）；已就地更正 STRATEGY_V6.md §四（「4 处全缺」→ **3 处全缺/6 处半成品/1 处完备**）。**环境实点**：`trafilatura`/`resiliparse`/`warcio`/`mlcroissant`/`fasttext` 均未装，`lxml` 6.0.2 / `bs4` 4.14.3 / `httpx` 0.28.1 / **`ray` 2.58.0 已装** / `streamlit` 1.55.0 已装；`data/raw/` 下**无任何 HTML 存档**（证实 RawDoc 缺失）。**12 个决策点要点**：①**层序按依赖而非兴趣分 4 组**（让证据可信→让决策可审计→让架构完整→让外部能用），**论证：三支柱的「迁移代价/阈值校准」必须有可信的真实分布数字，而现有真实数字被抽取缺陷污染（#65：2066 篇误杀 778 的绝大多数是空白膨胀而非真低中文占比）→ 补第 1 组不是摊薄，是给核心差异化清地基**；②**改写通道**（包侧新增 `Transformer` 并列协议＋`TransformResult`，`run_funnel` 增 `pre_stages` 可选参数默认空，**不改 Operator 协议签名**）；③**归一化层**（主仓，七规则含 `\r` 残留/U+2028 陷阱/空白折叠，**默认 opt-in 不改既有 config**，逐样本记 `normalize_log`）；④**RawDoc 内容寻址存档**（`data/raw/html/<sha2>/<sha>.html.gz`，红利=去重免费+可溯源+抽取器升级可全量重放）；⑤**抽取器三级链**（站点覆盖→trafilatura(`[extract]` extra 懒加载）→**stdlib Heuristic 永远可用**，CI 零新依赖）；⑥**判决书**（`verdict.jsonl` schema v1：op/decision/score/threshold/**rule 机器可读判据代号 / evidence 判据用到的量**/input_fingerprint/normalize/review 人审回填位/prov PROV-O 三元；**旁路产物，不传参零落盘 = 旧行为逐位一致**）；⑦**人审层**（队列三排序策略：边界带 > 算子分歧 > 配额；**硬闭环——推翻率 M/N 喂给阈值校准，无闭环不算完成**）；⑧**配比层**（manifest+ledger+域饥饿/过采样/源漂移三告警，**不做 RegMix**）；⑨**血缘层**（Croissant 1.1 + **`curation:verdictRecordSet` 记录级扩展**——业界只到文件级）；⑩**checkpointing 用装饰器不改 Executor 协议**（保住 γ 等价性验收语义）；⑪**可视化诚实定位**——搜索证实 **Data-Juicer 已有 `data_visualization_op_effect`（交互式参数调节+保留/丢弃样本展示）/`op_insight`/`overview_scan`/`tracer`（跟踪被过滤样本）/`dj-analyze`（相关热图+箱线图）/monitor/checkpoint/DJ-Sandbox+HPO**，**故「做可视化」本身不是差异化**；差异化只在三处可验证事实：**可视化的是「有没有依据」而非「长什么样」（阈值沙盘 = 分数分布 + 误杀预算线 → 推荐阈值，DJ 无预算线无推荐值）、决策可追溯到单条且能被推翻（DJ tracer 是运行期内存态不落盘；Croissant 只到文件级）、比业界更易上手（DJ 跑 demos 必须 clone + `pip install -e .[all]` + cd demos，且 ray 模式下 analyze/monitor/checkpoint 全部不支持；本项目目标 `pip install curation-eval[ui]` → `curation-eval demo` 一条命令）** → 新增 4 页签（⑦漏斗解剖与判决台账 ⑧阈值沙盘 ⑨人审队列 ⑩迁移代价对照），共 10 页签；⑫**UI 搬进包**（`curation_eval/ui/`，主仓 `showcase_app.py` 变薄壳，streamlit 进 `[ui]` extra，`demo` 不用静态 JSON 而是**现场跑 20 条合成样本生成判决书再起 UI**，真实数据走 `--run-dir`）；另 DJ 插件接入先 spike（entry-point 组名等**不许猜**，本表按 `data_juicer.ops` 占位，失败降级导出脚本）。**纪律**：每补层任务必须附带对照实验（A/B 或 A/B/C，禁止「层补好了但没数字说它有用」）；抽取层的对照实验**明确拒绝合成 HTML**（避免重蹈闭环自证反模式），改为按现成合规骨架重抓 200 页真实 HTML（robots + 1s 限速 + 幂等），并**预留阴性出口**（C ≤ B 时如实记录并把定位从「质量提升」改写为「换源成本降低」）。**分期 W1-W5 共 29 个原子任务**，W1（改写通道 + 归一化 + 判决书 + 2 页签）可立即动码 | 基线实点 **274+54**（包侧含 ray 5）；`web_sources.py` 实点推翻「抽取层缺失」；`data/raw/` 无 HTML 存档；DJ 已有 5 组可视化 demo → 差异化重新定位为「有依据 / 可推翻 / 易上手」；DJ 插件 spike 待定 entry-point 组名；W1-W5 / 29 任务；三处待用户裁决 | 待确认后动码 |
| 2026-09-20 | **V6 战略定位（docs/STRATEGY_V6.md）：全链路完整 + 一处极深（含反对意见）**——用户提出「算力妥协，其余全链路对标业界顶尖，或直接在业界管道上做修改，否则项目无存在意义；要符合 STAR 原则」。**逐条实点后给出判断，其中两条是明确反对**：**①「完整」本身不产生价值**——算力妥协下做全链路对标，产物只会是「每个环节都比业界差一点的复制品」，且面试官一眼看出是重实现 trafilatura/MinHash/fastText；**②「在业界管道上做修改」若指 fork 则自毁**——Data-Juicer 有 200+ 算子，fork 等于维护别人的代码库且评测资产被绑死；**正解是插件**：Data-Juicer **v1.5.5（2026-08-07）已新增 External OP Plugins——第三方算子可作独立 pip 包经 Python entry points 自动注册**，接入而非 fork，保持「可评测任何框架」的中立立场。**核心洞察（战略最关键一条）**：AI 能实现任何环节，所以护城河不是「我实现了什么」，而是「我决定不做什么、因为什么」——本项目已有的 PROOF_CHAIN §九 十条自认局限、明确「不做」清单（Next.js 真前端/W&B/算子市场）、δ 判官 κ≈0 判不合格、λ SemDeDup 诚实阴性，**这些是护城河不是欠账**。**算力妥协的真实边界（好消息）**：逐层盘点后只有 3 件事真需要妥协（绝对规模 10¹² tokens / 训练级裁判规模 7B×53 任务 / GPU 内核 cuDF-cuGraph）；**抽取层、归一化层、人审层、配比层、血缘层、checkpointing 全部纯 CPU 可做**，模型分类器与语义去重单卡可做 → 全链路对标在算力上基本可行。**架构对照**：业界 10 层（源接入/抽取/归一化/打分/去重/人审/配比/血缘/下游评测/执行），本项目 **3 处全缺（归一化、人审、血缘）+ 6 处半成品 + 1 处完备**（**就地更正**：初稿写「4 处全缺／抽取层完全缺失」有误——2026-09-18 实点发现 `src/mm_curation/data/web_sources.py` 已有源接入骨架（fetch+robots+限速+幂等）与一个正文抽取函数，真实差距是「站点耦合＋无原始 HTML 中间态」；归一化层缺失的真根因是**框架只有「打分→阈值」一条通道、没有改写通道**，见笔记 #65 原文）。**建议形态 = 全链路完整 + 一处极深**，那一处定为「**可审计的清洗决策**」三支柱：①**判决书**（每条被删样本一条机器可读裁决记录：op/score/threshold/判据依据/输入指纹，对齐 Croissant PROV-O 记录级血缘——业界只做到数据集/文件级）②**校准阈值**（从域分布 + 误杀预算反推，替代业界普遍拍脑袋的启发式阈值如 FineWeb fastText 0.65 / DCLM top-10%）③**迁移代价量化**（合成标定→真实分布的预期衰减区间，业界知道但无人公开量化）。三支柱全部不需要算力、全部建立在已有资产上（污染器+labels.dirty+dropped_by / threshold_scan / 三份真实报告）。**生态位判断**：V3 ι/κ/λ 已得出「curation-eval『回答算子好不好』生态位成立」——**这个生态位是真空的**（Data-Juicer 有 200+ 算子但不回答哪个净收益为正，NeMo 有 30+ 过滤器但不告诉你阈值定多少），**不要放弃它去做 Data-Juicer 低配版**。**STAR 重构**：用户的**问题不在 A（行动已很满），在 S——情景是虚构的**（「我造数据集→注入脏→清洗它」）；重构后 S = 业界阈值全是启发式拍定、迁移到真实分布无人量化（用 FineWeb 0.65 / DCLM top-10% 作业界证据 + 自有 100%→24% 实测），R = 误杀 16~28%→X% + 每条删除可审计 + 迁移代价可预测 + 用同一口径评业界算子排名。**三阶段路线**：①让结果可比（扩 held_out 119→千条、多 seed、接公开基准；**纪律：不新增任何算子/模态**）②三支柱（核心差异化）③补链条+对齐标准（**纪律：阶段1-2 未出数字不进阶段3**）。**明确不做并写进文档当资产**：绝对规模、训练级规模、GPU 内核、fork 框架、重造 Argilla/Lilac、鉴权多租户、RegMix 代理训练 | 业界 10 层 vs 本项目 4 缺 6 半；算力真实妥协点仅 3 处；三支柱全不需算力；Data-Juicer v1.5.5 external OP plugins 是接入而非 fork 的依据 | 本次提交 |
| 2026-09-20 | **业界对标：LLM 数据链路生产级现状 vs 本项目（docs/INDUSTRY_BENCHMARK.md，不避难点）**——用户要求对标真实生产级项目并直说差距。**先实点项目侧**：算子 **29 个**（`available_operators()` 实读，非记忆里的 19——那条是 V2 时期快照）、测试 **274+54**（2026-09-18 复点；包侧 54 含 5 条 ray 测试，ray 2.58.0 已装）、最大语料 302,002 篇/41,478 窗/2,106 对。**业界侧六类图谱**：①框架层——Data-Juicer（200+ 算子，**6400 核 2h 处理 700 亿样本**、1280 核 2.8h 去重 5TB、v1.5.5 把重复度过滤内存峰值 768→272 MiB）、NeMo Curator（cuDF/cuML/**cuGraph**，模糊去重 **16× vs CPU**、TCO 低 40%、1.x 重写为 Stage+Ray/Xenna）、datatrove（4 阶段 MinHash，**executor 自带 checkpointing/失败恢复**）；②成品数据集——**FineWeb 15T tokens/96 dumps**（**ablation 推翻了他们自己的全局去重计划，改 per-dump**）、FineWeb-Edu（Llama-3-70B 打分→回归小模型，砍 90% tokens 仍全面提升）、**DCLM-Baseline 240T 池→fastText 排序取 top~10%，model-based filtering 是关键（Core +3.5）**，7B 在 2.6T tokens 上 MMLU 64%；**MINT-1T 1.02T tokens/3.42B 图/420 万 CPU 小时**（HTML+PDF+arXiv 三套抽取器）；③人审工作台 Argilla（战绩：用其 UI 在原始 UltraFeedback 找出生成 bug，重造后 Notus 超 Zephyr）/ Lilac（Cohere、Databricks 在用）；④治理标准 **Croissant 1.1（2026-01-29）：PROV-O 机器可执行血缘 + DUO/ODRL 机器可执行许可，HF 上 70-80 万数据集已带元数据**；⑤配比科学 DoReMi/RegMix（ICLR 2025，只用 DoReMi 10% 算力）/Data Mixing Laws/**CausalMix（2026 清华，把配比重构为因果推断以抗数据池漂移）**+ 生产侧 mixture manifest/token ledger/域饥饿保护；⑥评测基准 DCLM（53 任务 × 412M-7B × 固定算力预算）。**14 维对标**（详见文档表）：**结构性缺失两条——前端 HTML→正文抽取层（trafilatura/resiliparse，DCLM 实测 resiliparse 高 2.5 分且快 8×）与后端模型裁判（下游模型当唯一裁判）**；**整层为空一条——数据配比科学**；**方向性差距一条——29 算子以启发式规则为主，而 DCLM 证明 model-based filtering 才是关键**（本项目已有 LoRA 判官 κ +0.560 与 llm_judge 算子，缺的只是接到漏斗主链）。**最锋利的一条**：本项目唯一抓到的真实数据 bug（笔记 #65 爬虫空白膨胀致 778 篇误杀）**正是缺前端抽取层导致的**，当时选择在算子层改 `chinese_ratio` 语义——补丁对但治的是症状，业界解法在抽取层。**项目真实定位**：规模轴落后 3-4 个数量级（不该比、比不了）；**证据严谨轴处开源上游**（污染器注入 ground truth + 算子级独立评测 + 主靶对靶 + 劣化注入实测变红 + 数字版本锁 + 十条自认局限，多数开源数据项目完全没有）；**消融驱动抉择这一点最接近业界**（分组消融找到「只有去重组显著」，与 FineWeb 用 ablation 推翻自身计划同源）。**排序建议**：①下游评测做扎实而非加算子（多 seed + held_out 119→千条 + 接公开基准）②人审队列③把 V3 判官能力用回数据筛选④补前端抽取层并用 #65 验证⑤输出 Croissant 元数据。**明确不建议**鉴权/多租户/GPU 内核/千核扩展 | 算子实点 **29**（记忆里的 19 已过期）；对标 14 维/业界 6 类；结构性缺失 2 条 + 整层为空 1 条 + 方向性差距 1 条；定位三轴：规模差 10⁴ / 证据严谨处上游 / 消融能力最接近业界 | 本次提交 |
| 2026-09-18 | **产品级差距审计（docs/GAP_AUDIT.md）：全仓实点，输出 P0/P1/P2/P3 四级缺口清单**——用户问「离真实可用的产品级项目还差什么」，逐条实测而非引用文档自述。**P0 三条会立刻咬人**：①`data/raw/real/`（383MB，`data/` 总 4.6GB）**未被 .gitignore 忽略**（`git check-ignore` 无输出，仅 `data/raw/*.jsonl` 等特定路径被忽略）→ 一次 `git add -A` 即入库，且 `labelled_df.csv` 207MB 超 GitHub 100MB 单文件上限会直接拒推；②`ingest_real_sensor.py`/`eval_real_sensor.py` **零测试**（`grep -rl` 无结果），而它恰是踩坑最密集处（分号分隔/采样率实测/子集混入三坑全靠注释记性），`_sampling_hz()` 是无保护的关键正确性逻辑；③本机 `make` 不存在且默认 `python`=WorkBuddy 3.13（无 numpy）→ Makefile 35+ 目标是文档主入口却全线不可用，报错方向还是误导的（提示 numpy 缺失而文档称已装）。**P1 九条产品化定义性缺口**：服务层 `grep auth\|token\|rate\|Depends` **完全无匹配**（零鉴权零配额）；`run_funnel(samples: list[Sample])` + `_read_csv` 整表读入 = **全内存批处理**（无流式/分块/断点续跑）；**无锁文件**（requirements 全为 `>=` 范围，本地绿/CI 红的结构性根源）；**无应用侧 Dockerfile**（只有 Dockerfile.airflow）；协议包**无 PROTOCOL_VERSION 常量**（schema 演进无法协商，而它自称单一事实源）；主仓 `version=0.1.0` 而项目已 V5、无 tag 无 CHANGELOG；**无审核队列**（PROOF_CHAIN §九8 已自认，今日证据把它从「可后补」变「必须有」）；50 scripts + 35 make 目标**无 `[project.scripts]` 统一 CLI**；无「真实数据+纯 CPU+十分钟」的外人可验证冒烟路径。**P2 八条真实分布适用性**（今日真实数据刚打出）：五算子判据全对合成形态写（默认窗 SKAB 五算子全静默）；阈值不可迁移且调参无解；**真实数据回答不了「漏了多少」**（SKAB 异常为人为切阀/C-MAPSS 是仿真/MetroPT-3 标签仅 1.97% 且不覆盖链路缺陷）；合成注入率 30% vs 真实 1.97%（**差 15 倍**）且缺四类真实形态。**P3 四条门面腐烂（今日实点）**：README.md:76 `175 + 40`、ROADMAP.md:428 `263+54`、showcase_app.py:383 硬编码 `267`、INTERVIEW.md:436「工程笔记 59 条」→ 实点 **273+49 / 66 条**，且 INTERVIEW 进度停在 V3（V4 医疗/V5 工业未进叙事）。**建议优先级**：①对齐判据↔真实形态（唯一「不修=产品不成立」项）②补人工审核队列（真实误杀率只能是上界，终裁必须靠人）③锁文件+数据忽略+门面数字纳入 verify-claims（小改动一次性根治）。**不建议现在做**鉴权/Docker/多租户/规模——先证明有效性再谈可用性。另「实点教训」一条：`ls docs_archive/*.md \| wc -l` 得 1 而真实 15（散在 7 子目录），**glob 层级深度会让实点本身给出错误答案** | 测试基线实点 **273 + 49**（README/ROADMAP/showcase 三处过期）；数据 4.6GB 中 383MB 未忽略；零鉴权；零锁文件；无应用 Dockerfile；笔记实点 66 条；清单共 P0×3 + P1×9 + P2×8 + P3×4 = 24 条 | 本次提交 |
| 2026-09-18 | **真实数据轨首跑：三个真实工业数据集全跑通（诚实阴性，合成→真实迁移代价量化）**——新增 `scripts/ingest_real_sensor.py`（真实数据集→窗口化+标签聚合→Sample 协议；DOMAIN_PACKS 定义的「漏斗外前置阶段」，支持 skab/metropt3/cmapss）+ `scripts/eval_real_sensor.py`（真实版算子级独立口径 + 漏斗串联，**不套用合成门限判合格**）。**三个数据集全部拿到并跑完**：SKAB（35 CSV/37,401 点/GPL-3.0）、MetroPT-3 全量（1,516,948 行/213 天，社区标签与官方失败表**逐行 100% 吻合** 29,954=29,954，检修时刻正确标 0）、C-MAPSS FD001（100 台×21 传感器）。**核心结论：合成轨数字在真实数据上基本不复现**——①SKAB 默认窗 256（=合成 WINDOW_SECONDS）下**五算子全部静默**（0 命中/0 误杀），压到窗 64 后 drift 才有数：真实召回 **24.0%**/误杀 **24.5%**（合成 100%/1.23%）；②MetroPT-3 drift 真实召回 **72.0%**（590/819）/误杀 **28.33%**，漏斗只保留 **68.4%**；③C-MAPSS drift 召回 52.1%/误杀 16.3%。**两条方法论级发现**：(a)**无标签 ≠ 干净**——MetroPT-3 上 stuck 的 1337 次"误杀"经抽检实为**7 段连续区间共 163 小时（占全期 3.19%）全通道同时恒定**的真实冻结/未标签缺陷 → 真实轨误杀率只能是**上界**，必须配人工抽检；(b)**同形态多含义**——C-MAPSS 上 stuck 误杀 **92.4%（7220/7817）**落在 6 个"整条轨迹恒定"的无信息通道（s1/s5/s10/s16/s18/s19，100/100 台全程不变），"完美平坦"既可能是卡死也可能是该通道本就不测量任何东西。**实测纠错三则**：SKAB README 写 `RateRMS`+逗号，实测 `Volume Flow RateRMS`+**分号**；UCI 页面 MetroPT-3 采样率自相矛盾（0.1Hz vs 1Hz），**实测中位 10 秒 ≈ 0.1Hz**（已改为从时间戳实测采样率，不再硬编码）；同目录 `Group_14_Clean_Data.csv`（59k 行子集）与全量文件混放会重复计数（已改为显式优选全量）。**镜像经验**：raw/codeload 当前不通，**ghfast.top 代理实测 ~300KB/s 可用**，jsdelivr 可用但大文件返 200+0 字节。报告 docs/REAL_DATA_REPORT.md（含 6 条行动建议：先对齐判据再谈阈值）；ruff 双脚本净；测试基线未动；未触碰在途文件 | 真实召回 24%/72%/52%，真实误杀 24.5%/28.3%/16.3%（合成对照 100%/1.23%）；真实脏率 1.97% vs 合成注入 30%（**差 15 倍**）；新增 2 脚本 + 1 报告 | 本次提交 |
| 2026-09-18 | **审计驱动补强：证明链两个关键实验 + PROOF_CHAIN.md**——用户提供 47 问审计清单（ground truth/因果/门禁/泛化四线），逐条盘点后补两个最关键缺口：①**多 seed 门禁稳定性**：eval_fhir/eval_industrial 加 `--seeds` 参数（worst_case_gate 保守聚合：最差召回/最差误杀/一票否决），3 seeds（42/7/2026）实测医疗最差口径 召回 100%/误杀 0.00%（分母 0/500）、工业 100%/1.23%（13/1056，seed42 实绩 1.04%），报告加 stability 字段+分母披露；②**随机删同比例 baseline**（回应最致命问「提升是清洗还是删样本」）：eval_random_drop_baseline.py 零重编码口径，漏斗删 521 条 R@1=0.575 vs 随机删 5 seeds 均值 0.447（0.417~0.500），**清洗净贡献 +0.128**——随机删反而略降，提升全部来自删对坏样本。docs/PROOF_CHAIN.md 证明链手册：四大关注点总览/竞品定位表（GE·Deequ·Cleanlab·NeMo·DataJuicer 生态位=评测闭环）/CI 覆盖矩阵（真跑 vs 离线如实）/门限=回归红线留劣化带的设定依据/数字→分母→出处映射表/最致命十问逐问索引/九条局限清单。查证落档：ppl 测试集代码级不相交（_load_sets 先切 test）；框架层零改动可证（V4α/V5α commit 均未碰 sdk/registry/runner）；最大自研模块 preference.py 614 行 | 多 seed 门禁双领域全 PASSED；清洗净贡献 +0.128 R@1；测试 +2（proof experiments）；文档 PROOF_CHAIN.md 为面试主弹药 | 本次提交 |
| 2026-09-18 | **V5 β 补强：演示门户视觉与包装升级**——用户反馈门户「丑、不像完整产品、外行看不懂」。调研 GitHub 官方 agent skill 库 anthropics/skills 的 **frontend-design** skill（71 行设计准则：主题隐喻先行、避开 AI 生成感默认款——奶黄底赤陶色/纯黑酸绿/千篇一律圆角卡+灰阴影/全大写眉标/中点连接符/按钮箭头；文案用户视角主动语态、空态给行动不道歉）。按其两段式流程（token 规划→对照反默认审查→实现）：①主题隐喻定为「数据净水厂」（脏数据=原水/算子=滤级/门禁=出厂验收），视觉走洁净室蓝绿（#F7F9FA 纸面/#0E7490 水青主色/绿红仅作门禁语义色），.streamlit/config.toml 统一仓库皮肤；②唯一大胆处=首屏净水流程条（原水→沉淀→过滤→深度净化→出水）+ 四领域实时门禁读数仪（合格出厂/待质检徽章），其余收敛；③包装层面向个人使用者：标题改用户语言「把脏数据，变成可信数据集」+「第一次来？30 秒看懂」（净水厂类比 + 召回/误杀白话）+ 三条角色路线（好奇/评估/接入自己的领域）；页签改白话（医疗数据/效果证据），指标卡标签白话化（故障召回=坏数据抓住了多少），空态改行动指令；④新增 .streamlit/config.toml（未入 gitignore，已入库） | 单测 4 + AppTest 真渲染（6 页签/7 表/10 卡）全绿；基线 267+54；ruff 双仓净；诚实注记：本会话浏览器插件 Node REPL 不可用，像素级截图目检未做，视觉终审留给用户 | 本次提交 |
| 2026-09-17 | **V5 β 执行收官：平台演示门户**——用户提出「可交互前端直观展示」；红线「不做真前端」在案 → Streamlit 总入门户（仓库第 5 个应用，定位唯一演示入口，四个存量应用不动、侧边栏跳转）。`scripts/showcase_app.py` 六页签：平台总览（四模态门禁卡实读报告 + 基座六件套表 + 测试基线）/ 图文 / 文本（去重基准表）/ 医疗 FHIR / 工业（后两个**现场重跑门禁按钮**——subprocess tail 白名单仅限 ~6 秒纯 CPU 脚本，judge_studio 同款）/ 证据链（finetune_eval 干净 vs 脏微调、ablation 去重组 ΔR@1、采样/κ 命令）。纯函数与 UI 分离（load_report 容错/pr_rows/gate_cards/ft_rows/ablation_rows/dedup_rows），报告缺失降级为「未生成 + 生成命令 + 耗时」；use_container_width 弃用告警清零 | 冒烟：headless HTTP 200 + AppTest 真渲染（6 页签/7 表格/10 指标卡无异常，门禁卡实读真报告 100%/0.00%）；纯函数单测 +4；基线主仓 **267** + 包 **54**；ruff 双仓净 | 本次提交 |
| 2026-09-17 | **V5 α 执行收官：工业传感器增强包（第四模态）+ 基座/增强包叙事落地**——设计表经用户确认后动码。**协议层**：MODALITY 登记 industrial_sensor + SensorSample 入包（一窗一 Sample=通道×256 读数 canonical JSON；maintenance_event 检修事件进同模态样本流——「计划检修静默=合法 vs 采集故障=异常」的业务事件源不外置，镜像 FHIR 多资源先例；v0.4.0）+7 包测；**数据层**：sensor_synth（3 类设备 6 台 12 通道、84 窗/通道=1008 读数窗+48 检修事件，AR(1) 走 seed，工况 schedule，检修窗真实缺席）+ industrial_quality 五算子（stuck/range 单样本；drift/unit/maintenance 批量 shardable=False；drift 基线中位数稳健化+lag-1 自适应 σ）+ sensor_contamination 五污染器（cal_offset 供体限定基线后）+ OPERATOR_TARGETS；**CLI**：funnel_industrial.yaml + eval_industrial 门禁 + Makefile eval-industrial；**规范**：docs/DOMAIN_PACKS.md 六件套（基座/包分工+扩展步骤+红线：不做插件系统、不改写语义）。**途中抓三个坑**：①事件窗 end 恰吞下一在岗窗（半开闭歧义）；②工况切换瞬态把稳态漂移检测的 z 顶到 6.5（生成器改为切换即重置稳态，瞬态归 V5 β 真实轨）；③全局统计算子的独立评测口径陷阱——其他类型灾难注入污染漂移基线致误杀虚高 155（中位数稳健化后 22，漏斗串联口径 1.04% 才是端到端承诺）；另修测试自身 union 语义 bug（批量主靶断言须按 kind 对各自算子） | 全量验收：1056+317，漏斗召回 100%/误杀 1.04% **PASSED exit 0**；五算子主靶 recall 全 100%；基线主仓 **263**(+34) + 包 **54**(+7) 零倒退；报告 operator_pr_industrial.{json,md}；四模态全通一个框架 | 本次提交 |
| 2026-09-17 | **V5 设计门：基座+领域增强包叙事确立，工业传感器包立项**——用户确认定位升级（多模态数据清洗与预处理平台 = curation-eval 基座 + 领域增强包六件套：适配器/算子/污染器/语料/config/门禁，FHIR 包为参考实现）；对 DeepSeek 调研方案做五点校正（模态已是三个非两个；fault_vs_maintenance 走 FHIR 多资源类型先例——检修计划事件进样本流建索引；公开数据集无 ground truth 只做 V5 β 真实底座轨；简历数字须回溯实测报告——钢厂案例/门限值不得当实绩；方向 B 属 findata 仓另立项）。V5 α 设计要点：一窗一 Sample（canonical JSON，镜像 FHIR）+ 五算子（stuck/range 单样本，drift/unit/maintenance 批量 shardable=False，同工况比较防合法 changeover 误杀）+ 五污染器 + 1200 窗合成语料（含合法静默）+ eval-industrial 门禁；红线重申：不做插件系统、不引入改写型算子语义（预处理走漏斗外阶段） | 设计表待确认后动码；V4 β（评测闭环）/V4 γ（CI 诊断）/V5 β（真实底座+PHM 证据）顺延另拆；基线 229+47 | 本次提交 |
| 2026-09-17 | **V4 α 执行收官：医疗 FHIR 模态零特例接入**——设计表经用户过目后动码（协议层→数据层→CLI→文档四层）。**协议层**：MODALITY_FIELDS 登记 `fhir_resource`（text=canonical JSON 扁平化，执行器/评测器零改动）+ `FHIRSample` 适配器入包（roundtrip 保真，v0.3.0）+ 7 包测；**数据层**：`fhir_synth.py` 确定性合成语料（500 条 P100/E100/O200/M100，引用闭合，约 8-10% 合法业务异常，内嵌 ICD-10/LOINC/ATC/UCUM/假名静态表）+ `fhir_quality.py` 五算子（phi_residual/code_validity/unit_normalization 单样本 + temporal_consistency/referential_integrity_fhir 批量 shardable=False，None=引用缺失不误杀语义）+ 五类合规污染器**落包侧 V2 协议套**（fhir_contamination.py，供体重抽模式；设计表原写主仓 V1 套，动码时发现仓库双注册表并存、V1 仅剩 contaminate.py 一个消费方，按 β 先例改投包侧）+ OPERATOR_TARGETS 五条；**CLI 层**：funnel_fhir.yaml + `eval_fhir.py`（算子级 P/R + 漏斗串联门禁 召回≥90%/误杀≤5%）+ Makefile eval-fhir。**途中抓三个真 bug**：valueQuantity 缺 unit 漏收集、ATC 正则层级写错（L NN LL NN）、birthDate 无时区与 aware 时间混比——均被「干净语料零误杀」断言当场拦截；污染器-语料打架一例（基线邮箱被自家 PHI 正则命中→脱敏基线去邮箱）。协作方在途 findata_health_stage.py 被 ruff --fix 顺带动了 import 排序（纯外观，如实披露） | 全量验收：500+150，五算子 precision/主靶 recall 全 100%，漏斗召回 100%/误杀 0% **PASSED exit 0**；漏斗串联 65→50 实测；主仓 **229**+35 / 包 **47**+7 全绿零倒退；报告 data/reports/operator_pr_fhir.{json,md}；笔记 #67 | 本次提交 |
| 2026-09-17 | **V4 设计门：医疗模态协议扩展 + 评测量化闭环**——外部任务书落为三期设计表入 design_tables.md：**α 医疗模态**（FHIR→Sample 走 text=canonical JSON 扁平化，执行器/评测器零改动；FHIRSample 适配器入包 v0.3.0 只增不改；5 医疗算子其中 temporal/referential 为批量 shardable=False；5 类污染器复用 ContaminationPlan；500 条确定性合成 FHIR R4 语料含引用闭合+合法业务异常；eval-fhir 内置门禁 召回≥90%/误杀≤5%）；**β 评测量化闭环**（benchmark_compare 对标 KramaBench 三层+Evian 分解——诚实边界：对协议不对数字、数据集不入仓；attribution_report 验收=复现去重组 ΔR@1 -0.017，文本 PPL 前缀曲线走 5000 条子采样代理口径；train-evidence 同 seed 双跑差 ≤0.5% 门禁）；**γ CI 失败诊断**（退步样本清单+算子触发差异表+artifact 上传，接三个门禁脚本）；δ 医疗端到端闭环依赖 α 实测另开设计门。在途避让：extraction.py/eval_judge.py/capability_matrix.json/runs/findata_health_stage.py 不触碰 | 设计表待用户确认后动码；基线主仓 194 + 包 40 只增不减；有意的两处字面偏离：算子收家族文件（仓库惯例）、benchmark 逻辑进 mm_curation/eval（非顶层 eval/） | 本次提交 |
| 2026-09-15 | **OPS w1 二段：首跑全通过 + 驾驶舱前端**——联网真跑验收全过：fetch 600519 落 10 条（血缘字段齐）→ 完整 ops_daily **exit 0**（600.6s：findata 采集 25 标的 6 万行 + 巡检信号 9/告警 7/健康分 72 + 文本 214 条 + 漏斗 214→163 保留 76.2%）。**第一天 audit 就抓到金融域校准问题**：chinese_ratio 丢弃 46 条（21.5%），抽样全是数字密集行情快报——#65 误杀的金融域变体（这次是阈值-域不匹配非 bug），进 docs/ops_backlog.md 首条（R6 清单启用）；磁盘水位告警同步触发（85.3%>80%）。前端 `ops_dashboard.py` 五页签（日报异常置顶/趋势曲线/审计柱状+抽样/语料筛选/手动运行实时日志——judge_studio 同款 subprocess tail 模式，只读落盘产物零额外采集，HTTP 200 冒烟过）+3 单测；f130e94 推送曾遇网络墙，恢复后补推成功 | 完整链路首跑 **exit 0**；主仓实点 **194** + 包 40（+3 零倒退）；优化清单 ops_backlog.md 启用（首条：chinese_ratio 金融域校准，连续观察 3 天后走设计门） | 本次提交 |
| 2026-09-15 | **OPS w1：运维飞轮一期（R0-R4）**——OPS PRD 落盘并经用户确认（docs/OPS_PRD.md：每条需求锚定一个「没有它必然发生的具体事故」，R0-R9 十需求 + Out 红线：不做实盘/前端/推送通道）；宿主仓库决策＝本仓内开发（抽离触发三条件写入 PRD §十一）。设计表 OPS w1 节落 design_tables.md 后动码：`ops_daily.py` 单入口壳（步骤表+产物校验+非零即停、**失败仍出日报**——管道吞错 exit 0 不可信教训对齐）；`fetch_finance_news.py` akshare 个股新闻→Sample JSONL（幂等=URL 去重、id=sha1(url)[:12] 跨运行稳定、单源失败不阻塞、股票池内嵌 findata UNIVERSE 25 只快照）；`text_funnel_finance.yaml` 独立配置（κ 教训防混入维基基线，阈值待金融真实数据轮校准）；`ops_install_schedule.py`（预览默认、--arm 显式武装，R8 环境冻结前不武装）。**发现两条**：findata 已自带 daily_pipeline（采集→巡检→推送→归档）——ops 直接复用，R1 告警推送免费获得；东财接口实勘超时一次→单源失败清单+次日增量兜底即对策 | 8 条离线单测全绿；实点基线主仓 **191** + 包 40（+8 零倒退）；dry-run 冒烟过（findata venv 自动定位成功）；待用户联网真跑验收（fetch --symbols 600519 → ops_daily --skip-findata 出首份日报） | 本次提交 |
| 2026-09-15 | **块3 V3 λ：SemDeDup 语义剪枝采样（诚实阴性）**——SemanticPruneSampler（faiss 球面 k-means 聚 64 簇 → 簇内按到质心相似度剔 ε → 存活池走既有分层配比；向量从 clean_v2 索引 reconstruct 零重编码，SamplingRecipe 增 extra 字段零破坏）；eval_sampling 加 --prune-fracs 消融；16 单测全绿（离群剔除/预算守恒/存活不足/可复现/无向量保守保留）。消融 ε{0.1,0.2,0.3}×budget{1200,1000,800}：仅 1200 档 ε=0.1 R@10 微胜（0.731 vs 0.706），其余全不敌且 ε 越大越差——**漏斗已在上游吃掉语义冗余，下游无冗余可剪反向证明去重质量**；ε=0.3 剪穿池子（存活 1081<1200）。笔记 #66：回归对比 random 全漂而 stratified 纹丝不动的虚惊——根因是 α 重跑后池子 1585→1586，shuffle 对长度敏感、分层配比稳健；**回归基线要带数据指纹** | 采样对比全表见 data/reports/sampling_semde_dup.{json,md}；主仓 183 + 包 40 全绿（实点） | 本次提交 |
| 2026-09-15 | **块2 V3 κ：规模拐点——单机全区间 local 胜**——语料扩量独立文件 text_corpus_1m.jsonl（100 万档，SHARDS 扩全 6 分片；--out 参数防混入 β 基线——extract_docs 按目标截断重跑会重复抽取）；ray_funnel_benchmark 参数化（--corpus/--tile/--out，流式读语料防 1M 档 read_text 内存尖峰）；scale_crossover_report 汇总三档出曲线。**实测：100k local 113.6s/ray 251.6s（2.21×）→ 300k 355.8/490.5（1.38×）→ 1M 1605.5/3749.8（2.34×，内存压力反升），每档等价性三口径全等（1M kept 774,424 逐 id 相等）——Ray 无回本点，回本条件是多机横向扩展**。测量纪律实测：与 judge 评测并跑时 100k local 虚胀 4.7×（24s→114s），计时实验必须独占机器。通用 1.5B 判官四任务补档（ι 前半）：δ κ 0→**0.247**（解析率 93.8%，「只改 base_url」实测成立）、ζ 0.047 / pref 0.517/0.467 ≈随机、ext 0.06-0.24 **反向**——通用放大不保证单调，域任务仍只有微调能救；矩阵已更新（capability_matrix.json）。INTERVIEW 杀牌 D 补 1.5B 行 + 新增「多大才该开 Ray」追问预案 | 报告 data/reports/scale_crossover.{json,md,png}；等价性三档全 ✅ | 本次提交 |
| 2026-09-15 | **块1 文档一致性收口（对标调研后立项三实验）**：先做同类项目对标调研（Data-Juicer / NeMo Curator / datatrove / dolma / RedPajama / GX / cleanlab / Argilla / distilabel / imagededup / text_dedup / Bespoke Curator），确认 curation-eval「回答算子好不好」生态位仍成立、Data-Juicer 2026-08 Juicer 模型正逼近域判官方向；随后收口四处 DVC 声明与实现的脱节（README 目录段 / requirements 注释弃用 / DAG 头注释 / ARCHITECTURE_V2 决策 8），统一为「数据不入库、全量可由脚本重生成」口径，INTERVIEW V2 追问预案补「为什么不用 DVC」话术；README findata 假链接 yourname→quannie255-star（ls-remote 实证仓库存在）；.gitignore 补 data/annot、benchmarks/pref_user_v1（oracle 产物 seed 可复现不入库）、uv.lock（空壳删除）；portfolio.html 单独入库。后续三块：V3 ι 判官翻案（1.5B 三任务 + η-b DPO）/ κ 规模拐点（local vs Ray 交叉曲线）/ λ SemDeDup 语义采样 | ruff 唯一报错在协作方在途 findata_health_stage.py（import 排序，未触碰）；实点基线主仓 **178 + 包 40**（本机含 ray、零跳过全绿；文档旧记 177 系历史口径）；本机 Git Bash 下 pytest 汇总行不显示，改以 `-p no:warnings` 进度点 + collect-only 求和实点 | 本次提交 |
| 2026-09-07 | **求职打包：作品集单页 + 门面数字对齐（数据岗侧重）**——实点测试基线发现三份文档集体过期（README 写 157+40、INTERVIEW/DEV_PLAN 写 175+40，**实为 175+35**：包 `test_ray_executor.py` 5 条被 `importorskip("ray")` 从 collect 阶段摘掉，CI 未装 ray 故口径一致）；三处基线同步改为 175+35 并注明「装 ray 才是 40」。README 补**「面试官 30 秒入口」**（5 个数 × 各自回答什么质疑）+ 核心结果表补 V3 η/θ 两行 + 状态行 / V3 定位段 / 灵魂叙事 / 快速开始 §9 全部对齐到 θ。新增 `docs/portfolio.html`——求职作品集单页（JD 映射表 / 数据岗三张牌 / 训练级证据 / 三级演进 / 三处阴性边界 / 可复制简历 bullet / 5 条追问预案 / 复现命令，打印友好）。笔记 #65 | 测试基线 **175 + 35**（另 5 条 Ray 需 `pip install ray`）；README 口径「V3 ζ 收官」→「V3 全阶段收官 ζ/η/θ」；ruff 唯一报错在协作方在途的 `scripts/findata_health_stage.py`，未触碰 | 未提交——避让在途的 `extraction.py` / `findata_health_stage.py`，待协作方收工后一并提交 |
| 2026-09-06 | **chinese_ratio 语义修复落地（#65 收口）**：盘点确认算子框架为纯打分器（无文本改写通道），弃「前置归一化算子」改修算子语义（去空白后计占比，<20 行 + 3 测试）；真实漏斗重跑 **2066 → 2031（保留 61.5% → 98.3%）**，chinese_ratio 拦截 778 → 5（精确命中审计预测，含 1 篇 GBK 乱码）；二次红利：下游 text_minhash 新增发现 12 条真实转载（17→29）；主仓 177 + 包 40 全绿 | 残留 2 篇边缘误杀（法规 0.29 压线 / 人名密集 ppl）如实记录不追调 |
| 2026-09-06 | **真实脏数据试跑（用户发起）**：2066 篇真实爬取新闻过文本漏斗 → 1271（61.5%）；审计发现 chinese_ratio 拦的 778 篇里 773 篇是**空白膨胀误杀**（被拦篇空白占比中位 77.2%，爬虫抽取缺陷；去空白后真实中文占比中位 59.9%，真非中文仅 5 篇），text_minhash 拦 17 条真实转载对；根因 = 维基校准管线缺前置归一化算子，域迁移照出缺口（笔记 #65）；修复方向入候补池 | 漏斗在真实数据上结果碰巧对、机制错；保留集空白中位仅 1.4%（双峰） | 见 git log |
| 2026-09-06 | **θ 执行收官：偏好判官工坊达标（模拟用户账）**——五步向导 judge_studio.py（导入→标注→训练→评测→试用，冒烟 200）；标注 v2 全文落盘修断链；泄漏检查中文两层失真（模板假阳性 + 字节 4-gram 塌缩，pref_news_v1 的 150/150 假泄漏由此而来，笔记 #63）；`--generic` 静默跳过 adapters 的坑修掉；e2e 两轮：250 条 0.532≈通用（欠训练）→ 加量 550 条（--freeze-eval-from 冻结考卷）**0.839 达标**（学习曲线笔记 #64，向导建议量改 500）；语料增量爬取 1558→2066 | 模拟用户账：通用 0.532 → 判官 **0.839**（对照 0.80 vs 0.40）；主仓 175 + 包 40 全绿 | 本次提交 |
| 2026-09-06 | **θ 设计门：偏好判官工坊（真人偏好闭环 + 大众可用向导）**——用户确认方向（个人偏好判官 + Streamlit 全流程向导）后走设计门。现场盘点发现硬缺口：platform_app 标注页只落 `cand_a_chars` 不落候选全文——存量真人标注无法还原 DPO 对，闭环事实上断裂。设计四决策：①标注 v2 schema（全文+变体元数据+labeler=human\|oracle）；②η-a 配方全复用（最小对纪律内建、DPO 脚本零改动、按偏好对 holdout 25%、REJECT 只进统计）；③验收两本账（模拟用户 ≥0.75 先行 / 真人 ≥150 对 ≥0.70）+ 学习曲线实验（50/100/200 点击）；④新入口 `judge_studio.py` 五步向导（导入→标注→训练→评测→试用），报告按 benchmark 名落盘防覆盖 | 无新数字（设计门）；设计表已入 design_tables.md θ 节，等用户确认后动码；不触碰 η-b' 在途 extraction.py | 本次提交 |
| 2026-09-03（协作方·第八会话） | **V3 ζ 达标叙事回填 + 工程笔记编号撞车修复**：glm ζ 收官（48ce6fc）后发现**笔记编号冲突**——其 ζ 根因两条（协议错位/completion 窗外）用了 #56/#57，与此前 git push 根因（#56）/phash 合成图（#57）撞号。重编号为 **#58/#59**（保持文件出现顺序），修正 5 处引用（PRD ζ5 / ROADMAP ×2 / RUNBOOK 微调红线 / DEV_PLAN ζ 行）；条数 57→59（INTERVIEW ×2 / ROADMAP / README）。**INTERVIEW 第八节回填**：标题与诚实标注改「ζ 收官」，进度表 ζ0-ζ5 全 ✅，新增验收数字段（通用 κ=-0.024 → 微调 **+0.560**，P=0.706/R=0.960/解析率 100%）与「三次训练的根因链」小节（v1 -0.18 协议错位 → v2 -0.007 completion 窗外 → v3 +0.560，loss 是模型在学『某个』任务的证明）；追问预案补验证句；开篇/STAR/杀牌/技术栈/第九节共 6 处「进行中」语气对齐。**README**：状态行 + 核心结果表加 V3 ζ 行 + 灵魂叙事补 V3 段。ROADMAP 测试基线 151+40 → 157+40（glm 用了 ε2 提交前旧数，实点修正） | δ 阴性 → ζ 达标的完整叙事线（协议筛人 → 微调达标）在 INTERVIEW 成形；笔记编号唯一性恢复；测试基线 157+40 | 本次提交 |
| 2026-09-03（协作方·第七会话） | **ε2 阈值回归门收官（ε 阶段最后一个真缺口）**：实点发现 DEV_PLAN 的 ε2（threshold_scan 曲线回归门）从未实现——ROADMAP 标 ✅ 的 ε2 是另一编号体系（损伤强度标定）。新脚本 `scripts/threshold_regression_gate.py`：扫描曲线 import `threshold_scan.scan_operator`（单一定义源，两处永不漂移），对冻结基线 `configs/threshold_baseline.json` **逐点比对**（recall 偏移 >0.05 / 误杀偏移 >0.02 即红），锁轻依赖双主算子 **minhash_lsh（datasketch）/ phash_near（imagehash）**（blur 要 opencv、clip/semantic 要编码器，不入 CI 门禁）。合成语料沿用两条已标定配方（文本 #49 大词表 / 图像 #57 8x8 随机块 + 1~5% 裁剪）。**门禁绿 13s**；基线曲线健康——minhash 拐点在 0.7→0.75（1.0→0.6125）、phash 在 20 过松时误杀飙 26.5%。测试 `tests/test_threshold_regression_gate.py` 5 条（匹配绿 / recall 漂移红 / 误杀漂移红 / 阈值点缺失红 / 语料 label 契约）。已接入 data-ci.yml（补装 datasketch） | CI 三层锁：代码逻辑 → 单点质量数字 → **整条阈值曲线**；换库版本（imagehash/datasketch）静默漂移先被曲线抓住。测试基线 **157+40**（实点；GPU 忙全量未真跑，新增 5 测单独真跑绿）；ζ3-ζ4 在途文件未触碰 | 本次提交 |
| 2026-09-03（协作方·第六会话） | **ε 门禁扩到图像去重（候补池收官）**：新脚本 `scripts/data_ci_image_benchmark.py` 锁 md5_exact / phash_near——2800 张合成图（2000 base 8x8 随机块放大 / 300 exact 字节复制 / 500 near 裁剪+JPEG 重编码注入）真跑漏斗算子，**exact 1.0 / near 0.994 / 误杀 0/2000 → GATE PASSED（去重 40s）**；劣化注入 --crop 0.80,0.90 实测 near 0.152 → **exit 1**；已接入 data-ci.yml。**标定三轮收窄损伤**：V1 near_duplicate_image 的 4~10% 裁剪参数用在块状合成图上 15.6% 距离超阈 12（块网格被裁剪错位，比真实照片敏感），收到 1~5% 后 p90=8，门限 0.97 留 2.4pp 余量——「先标定生成器再定门限」的图像版实践。RUNBOOK §1.9.1、INTERVIEW 杀牌 E 回写 | 候补池两项全收官；数据 CI 双模态全覆盖；劣化注入红、正常绿；ζ3-ζ4 在途文件未触碰 | 本次提交 |
| 2026-09-03（协作方·第五会话） | **图像漏斗 × Ray 等价性验证（候补池第一项收官）**：设计表入 design_tables.md——γ3 三口径 + 新增第四口径（dedup 标记 duplicate_of 逐 id 相等，只比 kept 集看不出"簇代表换了谁"）。实现 `scripts/ray_image_funnel_benchmark.py`（镜像 γ3 脚本，GPU 算子剔除同套路）+ 等价性测试 `tests/test_image_ray_equivalence.py`（importorskip 守卫）。实测 **2106 条全量双跑四口径全等**（kept 1608 / StageStat / 逐 id 分数 0 不一致 / dedup 标记 324 条），local 65.4s vs ray 137.3s（init 11.0s）。**附带发现**：id 排序使簇代表 = 字典序最小 id，注入复制品（id 小）会成为簇代表而非原始图——与污染模块「种子在前」约定相反，审计 duplicate_of 指向时需知晓（kept 质量不受影响）。**坑**：合成测试图两连败（噪声图与单频光栅在 phash 眼里都互相塌缩），终版 8x8 随机块 + 测试内自校验种子（两两距离 ≥16），工程笔记 #57；ray object store 2GB 在内存紧张时 init 失败，降到 1GB | 四口径全等，图像模态等价性债收掉；测试基线 **152+40** 全绿（GPU 空闲，全量 pytest 已跑）；ζ3-ζ4 在途文件未触碰 | 本次提交 |
| 2026-09-03（协作方·第四会话） | **根治 git push 挂起 + 候补池盘点**：(1) 定位推送挂死为凭据环节而非网络——`git ls-remote` 秒回但 push 无输出直到 timeout(124)；根因是 system 层 `helper-selector` 取不到凭据后 git 回退终端交互询问、stdin 阻塞，且 `-c credential.helper=X` 是**追加非替换**（system 那条仍先执行，故单加无效）。修法：global 层空值重置 + wincred，**裸跑 `git push` 现已可用**，卡住的 57d7470 已推送（7a29bcb..57d7470）。RUNBOOK 新增 §0.1「Git 推送排障」分列 SSL / 凭据两个故障。(2) 候补池盘点：**跨模态统一债务记录作废——19/19 算子 V2 元数据全齐**（纯图像 8/纯文本 7/双模态 4），图像漏斗早已走 Sample+Executor，「α 只迁 12 算子」是过时快照。同时翻出两个真缺口并立为候补池前两项：图像漏斗从未在 Ray 下验证等价性（3 个 shardable=False 全局算子是高风险区，phash_near 大概率复现 γ3 的簇代表块序坑）、ε 数据 CI 未覆盖图像去重。(3) 测试基线实点为 **151+40**（原记 133+34 / 140+40 均错，已加「别凭记忆填」的提示）。工程笔记 #56 | 推送恢复 + 候补池记录与仓库现状对齐；**仅动 docs/**（RUNBOOK / DEV_PLAN / ENGINEERING_NOTES），未触碰任何代码，持续避让 ζ3-ζ4 在途文件；本会话未跑全量 pytest（GPU 被 ζ3 训练占满，7855/8188MiB） | 本次提交 |
| 2026-09-03（协作方·第三会话） | **INTERVIEW.md 升级到 δ/ε/V3**：新增杀牌 D（LLM-judge κ 准入门槛——阴性结果敢讲：κ(t) 全阈值域 ±0.015 / judge vs L1 0.056 / L1 参照 0.565）+ 杀牌 E（数据 CI 门禁：exact 1.0 / near 0.954 / 误杀 0 / 0.5s，劣化注入实测 exit 1）；新增「八、V3 个人微调平台」（立项依据＝δ 阴性结果、差异化定位表、ζ 进度表、V2 资产复用表）；**Q5「为什么不用 LLM-judge」由「ROADMAP 预留接口」改写为「已实装但 κ 验收不合格」**（避免与第七节自相矛盾）；STAR 表补 δ/ε 两行并修复 V2 行的三列表格渲染丢列 bug；简历 bullet +3、技术栈 +5、电梯演讲补 δ/ε/V3 三段；笔记条数 53→55；收尾独立为第九节 | 文档口径与 δ/ε/ζ 现状全面对齐；ruff check All checks passed；**仅动 docs/INTERVIEW.md + docs/DEV_PLAN.md，未触碰任何代码**（避让 ζ3-ζ4 进行中的 `finetune_judge_lora.py` / `benchmarks/builder.py` / `tuning/judge_data.py`） | 本次提交 |
| 2026-09-06 | **业务层三件套**：①能力矩阵第二行——1.5B SFT 退化路径（DPO 在 8GB 显存不可行，258s/step）实测 0.59/0.48/0.52 仍 ≈ 随机，**能力悬崖在 1.5B 之后**，矩阵固化 benchmarks/capability_matrix.json；②判官成本核算——本机 0.5B 判官 ¥0.11-0.32/万条 vs API ¥26 vs 人工 ¥2500；③平台控制台 platform_app.py 四页签（能力矩阵渲染/成本计算器/A-B 偏好标注工具/命令速查），冒烟 200；Makefile platform/judge-cost 目标 | 成本差：本机 vs API ≈235×、vs 人工 ≈23000×（质量以矩阵为准） | 见 git log |
| 2026-09-05 | **η-b 执行（首轮未达标·诚实记录）**：抽取忠实性判官——预研修正（漏排 pref 占用，604→实算空闲 310→增量爬取语料至 1558）；三损伤构造（数字篡改/幻觉注入/关键遗漏，最小对内建）+ ext_news_v1 冻结 80 题；DPO 训练（窗口权衡实测：事实产量 800-1500 字平坦，取 900 换可训性 19s→55s/step）；出数 0.53/0.50/0.41 ≈ 通用基线 0.47/0.41/0.41——**阅读对齐超 0.5B 能力下限**，转化为「判官能力矩阵」业务概念（笔记 #62）。期间修复 runner 泄漏的 TypeError（管道吞错实测教训：exit 0 不可信） | 三损伤命中率 ≈ 通用；1.5B+ 重训为后续选项 | 见 git log |
| 2026-09-04 | **η-a/η-c 执行闭环**：η-c 域外泛化交付（维基 OOD 198 条：κ 0.178，召回保持/精确率腰斩，语料增至 1439）；η-a 偏好闭环——数据构造（S/F 变体 + persona-oracle + 结构性排除）→ DPO 双判官（trl 0.17，两轮：首轮学崩根因=chosen/rejected 差异面被 reason 模板吸收→最小对改造）→ 冻结 benchmark 出数达标；期间修复评测解析器（多行 JSON 截断→None 伪装学崩，54/60）与 DPO 性能（152s→19s/step）。笔记 #60-61 | **命中率 0.933/0.867 + 分歧率 0.783 达标**；域外 κ 0.178；157+40 测试绿（η 新增 5+6） | 见 git log |
| 2026-09-04 | **η 阶段设计门**：按 PRD §九三条差距立项 η；η-a 设计表落 docs/design_tables.md——偏好操作化为「详略维度 + persona-oracle 协议」（v1 非真人标注，如实声明），DPO 主案（trl 未装为首要风险，SFT 退化案已备），验收 = 命中率 ≥0.75 + **分歧率 ≥40%**（偏好进信号的直接证据）+ 内容对照题防长度 shortcut；η-c 热身方案含 5 行 CLI 补丁披露。等确认后动码 | 无新数字（设计门） | 本提交 |
| 2026-09-03 | **V3 ζ 首战闭环（ζ0-ζ5）**：PRD 落盘；新闻爬取器（705 篇，robots/限速/幂等/结构解耦，图集页推荐位混入实测修复）；benchmark 构建器（冻结 300 条 + 三重独立性 + SFT 结构性排除）；LoRA 微调器 + 实验 ledger；评测 runner。**三次训练的根因链**：v1 κ=-0.18（裸 prompt 练/模板考，笔记 #58）→ v2 κ=-0.007（completion 窗外截断，loss 健康的静默任务替换，笔记 #59）→ v3 **κ=+0.560/R=0.960/解析 100%** 达标 | 通用 -0.024 → 微调 +0.560；主仓 157 + 包 40 绿 | 见 git log |
| 2026-09-03 | **V3 立项（ζ 个人微调平台）**：与用户对齐产品愿景（个人化「数据→benchmark→模型」三步），确定锚点「专属数据判官」+ 本机算力路线；PRD 首次落盘 docs/PRD.md；ζ1 开工（新闻源爬取） | 通用 0.5B judge κ≈0.013 → 目标 ≥0.5 | 见 git log |
| 2026-09-03 | **ε 数据 CI + V2 收官**：data_ci_benchmark.py 合成语料门禁（exact 1.0/near 0.954/误杀 0，劣化注入实测变红）；损伤强度标定（del=2 时 J 跌破捕获带——门禁测实现不是测生成器）；data-ci.yml 双徽章；RUNBOOK/ROADMAP/README 收官账。另：推送 SSL 证书墙 → schannel 后端修复（RUNBOOK 记录） | 门禁 0.5s 跑完；near 0.954 vs 门限 0.90（4.5pp 余量）；V2 五阶段全落地 | 见 git log |
| 2026-09-03 | **γ Ray 执行层完成（γ0-γ4）**：spike 定路线（Win 原生 ray + 对象列传输协议）；RayDistributedExecutor + 共享批量语义函数 + runtime 配置；等价性测试 5 条；10 万档双跑基准。γ3 首跑失败（条数相等但集合不等——去重簇代表依赖块序）→ 批量算子 id 规范化排序修复（同场发现协作方未提交的 CI 门禁变更，合并提交）。笔记 #51-53；spike 临时脚本已删 | 等价性：kept 集/StageStat/逐 id 分数全等；10 万档 local 24.0s / ray 89.6s（含 init 7.8s）、kept 46,122；包 34 + 主仓 133 绿 | 见 git log |
| 2026-09-03（协作方·第二会话） | INTERVIEW.md 升级到 V2：新增「七、从「一条管道」到「一个框架」」（升级动机 / 三张杀牌：α 协议收口+反向吃狗粮、β 第二模态零特例接入、γ 执行层可替换 / 5 条 V2 追问预案 / 一句话收尾）；同步电梯演讲（补 V2 一拍）、STAR 结果表（+4 行 V2 数字）、技术栈表、简历 bullet（+4 条 V2）；工程笔记条数表述对齐 53（README 同步 50→53）。**避让原则**：未触碰 README 主体与任何代码文件，δ 状态与测试基线留给 δ 负责人回填 | 文档口径与 ROADMAP β/γ 数字全面对齐；质量门复核：主仓 140 + 包 40 全绿（含未提交的 δ 测试）、ruff check/format 干净 | 本次提交 |
| 2026-08-31 | β T0-T7 收官：T6 微调首跑阴性（+0.4%）→ 损伤集重造（UTF-8→GBK 乱码/复读/重复字符/截断，剂量 100%）重跑达标；30.2 万全量漏斗；文档回填；工程笔记 #44-50 | 去重 10 万档 exact 1.0/near 0.97/21s；微调 clean 7.16 vs dirty 7.70（+7.5%）；漏斗 302,002→181,980（保留 60.3%）；129+29 测试绿 | 73d0227 |
| 2026-08-下旬 | β T0-T5：语料接入、4 污染器 + 6 算子对靶修复、perplexity（GPT-2 zh 权重入口化）、dedup_fast 三 bug 修复（U+2028/模板簇撑破 LSH 桶/union-find 合并方向） | near 召回 0.42→0.9714（合并方向修复是决定性一跳）；CVE 墙→ensure_local_gpt2() | 前序提交 |
| 2026-09-03 | 建 DEV_PLAN.md + AGENTS.md 协作入口；tasks.md β 状态对账 | — | 67da7fc |
| 2026-09-04 | **README 门面升级到平台定位**：标题「多模态图文数据清洗与向量检索 Pipeline」→「多模态数据质量平台：清洗管道 → 向量检索 → 个人微调」，首屏加三级演进引语（V1 管道 → V2 框架 → V3 平台）+ V3 定位段；ROADMAP 标题同步；漏斗口径对齐可复现产物（99.8%/2.16%）；新增 docs/INTERVIEW_SELFTEST.md（41 题四层自测题库）；README 首屏加 Demo GIF + 4 截图（9248fda/378c5ae/本次提交） | 投递就绪：门面/徽章/数字/复现/自测五件套齐 | 见 git log |
